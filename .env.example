PORT=3000
# Windows 与 Ubuntu 统一使用 SQLite；数据库是一个真实文件。
DATABASE_PATH=./var/blog.sqlite
MEDIA_PATH=./var/media
BACKUP_PATH=./var/backups
CONFIG_ENV_PATH=.env
# 在本机生成随机密钥后填写，至少32字符。
SESSION_SECRET=
# 首次建库时设置管理员账号；普通注册用户不填写此项。
ADMIN_USERNAME=
ADMIN_PASSWORD=
