PORT=3000
# Windows 与 Ubuntu 统一使用 SQLite；数据库是一个真实文件。
DATABASE_PATH=./var/course-blog.sqlite
MEDIA_PATH=./var/media
BACKUP_PATH=./var/backups
CONFIG_ENV_PATH=.env
# 在本机生成随机密钥后填写，至少32字符。
SESSION_SECRET=
# 教师首次建库时设置；普通学生不填写管理员账号。
ADMIN_USERNAME=
ADMIN_PASSWORD=
