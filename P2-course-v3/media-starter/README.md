# 媒体任务起步工程

先按Windows-guide.md准备环境。终端进入本目录，运行`npm.cmd install`和`npm.cmd run setup:course`，再开两个终端运行后端和前端。

只修改`src/components/ArticleMedia.vue`：任务一完成图片；任务二从音频或视频中选择一种。代码、解释和验收步骤见补充PPT第8—11页。完成后用`complete`工程对照，不直接复制整个答案。
