# lesson-11｜媒体、备份与 Ubuntu 部署

本模板由 `../工程基线` 生成，去掉了本课要你自己写的关键代码。

- 完整课堂步骤见《第11课 课堂操作手册》，不要只看本文件。
- 代码里搜索 `第11课 TODO` 就能找到本课全部 TODO。
- 依赖、`.env` 和数据库文件都没有放进模板，按手册自己安装和生成。

开始之前先确认 Node 版本：

```cmd
node -v
```

必须是 `v24.` 开头。
