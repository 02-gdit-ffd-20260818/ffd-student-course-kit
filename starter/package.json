{
  "name": "ffd-p3-community",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "vite",
    "check": "node scripts/check-project.mjs",
    "test:unit": "node --test tests/*.test.mjs",
    "test:component": "vitest run",
    "test": "npm run test:unit && npm run test:component",
    "build": "vite build"
  },
  "dependencies": {
    "echarts": "^6.1.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
