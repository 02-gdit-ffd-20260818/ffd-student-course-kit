# 第03课_数组驱动作品｜学生任务单

## 本次结果

把作品保存在数组中，完成可重复渲染并处理空数据。

## 从哪里开始

领取学生起步/lesson-03。第2—6课先按《版本衔接表》保留上一课个人内容，继续在原Git仓库开发。

```powershell
npm ci
npm run check:starter
npm run dev
```

先确认浏览器能打开本地页面。未完成TODO时，check:lesson预期失败；这与启动失败不同。所有命令都在含package.json的当前项目目录运行。

## 你必须亲手完成

完成 app.js 中 renderProjects(items) 的 TODO-L3；在 data.js 中增加自己的作品记录。

定位文件：data.js, app.js, scripts/project-service.mjs。

## 分段操作

1. 前25分钟看需求、回答预测题，跟随教师完成最小例子。
2. 25—50分钟完成第一段：data.js可导入；能读取对象字段与数组长度。
3. 50—60分钟根据检查结果补讲，再独立修正。
4. 60—100分钟完成第二段：渲染正常、空、重复调用，数量与卡片一致。
5. 100—120分钟验证、修复、提交；发布节点更新原网站。
6. 最后15分钟展示个人变化，回答概念题与应用题。

## 操作提示

### 第一段操作：读取与修改数据

修改 data.js 的作品名称与简介。
增加一条含 id、name、skills 的记录。
在 renderProjects 中打印参数和长度。

### 循环让每条数据成为卡片

for...of 依次取出每个作品对象。
createProjectCard 已提供，返回一个卡片节点。
append 把节点放入容器。

```javascript
for (const project of normalized) {
  projectList.append(createProjectCard(project))
}
projectStatus.textContent =
  `项目数量：${normalized.length}`
```

### 第二段操作：完成渲染函数

按顺序完成标准化、清空、空态、循环、数量。
正常数据出现卡片。
修改数组后再次调用，数量保持正确。

### 检查点与常见错误

卡片重复：检查渲染前是否清空。
页面全空：看 Console 的第一条错误。
导入失败：核对 ./data.js 的位置和文件名。
名称缺失：观察标准化后的默认文本。

### 三个可复现的测试

正常：两条记录得到两张卡片。
边界：空数组显示空状态与数量0。
重复：先渲染一条，再渲染两条，最终是两张。

## 完成后的命令

```powershell
npm run check:lesson
npm run check
npm test
npm run build
git status
git diff
```

检查本次修改后，选择相关文件暂存、提交。提交命令与发布步骤见《Git与发布手册》。第1课首次发布，第6课发布Vue里程碑，其他课可随正常推送更新。

## 个人变式

加入两个作品后再渲染一次；清空数组后显示空状态，恢复数组后卡片恢复。

## 测试记录（自己填写）

| 情形 | 输入或操作 | 预期 | 实际 | 修复与复测 |
| --- | --- | --- | --- | --- |
| 正常 |  |  |  |  |
| 边界 |  |  |  |  |
| 失败或恢复 |  |  |  |  |

## 提交

本课运行结果、个人commit、离堂题回答。发布课增加公开URL和一次其他设备访问结果。真实平台失败时提交本地结果与故障记录，补交时间按教师安排，不能把本地成功写成公网成功。

## 离堂题

1. 对象与数组分别表达什么？
2. 为什么渲染前先清空旧节点？
3. 一条数据的 name 为空时，页面应该怎么办？
