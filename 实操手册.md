# 第01课实操手册：名片上线

从领取初稿、具体修改到推送自己的远程仓库

适用：P1 操作详解课件 v1.2；Windows PowerShell；每一步做完并确认结果，再往下走。

阅读顺序：步骤 1—4 领取并运行；步骤 5 具体开发；步骤 6 检查；步骤 7—10 提交与远程核对；步骤 11 练习再次推送；步骤 12 发布网站。课堂仍由 PPT 按原有页序主导，手册用于实操时查阅。

## 1. 本课入口与操作约定

本课资源：[第 01 课学生资源](https://github.com/02-gdit-ffd-20260818/ffd-student-course-kit/tree/p1-guide-01-v1.2)。打开后，网页左上方分支应为 p1-guide-01-v1.2。仓库内的 starter 是有意留出课堂任务的初稿。

本手册可以直接在 GitHub 阅读。领取后，在 VS Code 中打开“实操手册.md”，按 Ctrl+Shift+V 预览。Word 版供阅读和打印，复制长命令优先用文本版。

所有 powershell 代码框都在 Windows PowerShell 输入；html、css、javascript、vue、markdown 代码框写进指定文件。代码框外的中文说明不输入终端。一次执行一个代码框，出现红色错误先停下来。PS C:\…> 是提示符，不需要输入。新开终端后变量不共享，要重新执行相应的进入目录命令。

你会遇到两个不同的仓库：老师的 ffd-student-course-kit 用于领取材料；自己的 p1-portfolio 用于保存和推送成果。只在 p1-portfolio 内开发。不要把老师资源仓库填成自己的 origin。

课前已有：Git for Windows、Node.js 24、VS Code，以及能在浏览器登录的个人 GitHub 账号。

首次准备：浏览器分别打开 [Git 下载](https://git-scm.com/downloads/win)、[Node 下载](https://nodejs.org/en/download)、[VS Code 下载](https://code.visualstudio.com/Download)，安装 Windows 版本。Git 安装时保留 Git Credential Manager；Node 选择 24 系列安装包。安装完关闭并重开 VS Code。GitHub 账号在 [注册页](https://github.com/signup) 完成注册与邮箱验证。

打开 VS Code，点击顶部“终端 → 新建终端”。如果终端名称不是 PowerShell，在终端右上方“+”旁的小箭头选择 PowerShell。没有打开工程也可以先领取资源。在下方终端依次检查：

运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：任意目录

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 显示 Git 版本号，确认 Git 已安装。
git --version
```

命令解释：显示 Git 版本，确认可以领取和提交代码。

看到这些再继续：有版本号；如果“无法识别”，先完成安装并重开终端。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：任意目录

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 显示 Node.js 版本号，本课程统一使用 v24。
node --version
```

命令解释：显示 Node 版本，本课统一使用 v24 系列。

看到这些再继续：有版本号；如果“无法识别”，先完成安装并重开终端。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：任意目录

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 显示 npm 版本号；npm.cmd 是 Windows 的命令入口。
npm.cmd --version
```

命令解释：显示 npm 版本；使用 npm.cmd 可避开 npm.ps1 的执行策略问题。

看到这些再继续：有版本号；如果“无法识别”，先完成安装并重开终端。

## 2. 领取本课模板

位置：Windows PowerShell；起始目录任意。先建立统一的 web-work 工作目录：

运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：任意目录

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 确定工作目录：本人 Windows 用户文件夹下的 web-work。
$workRoot = Join-Path $env:USERPROFILE 'web-work'
```

命令解释：把本人 Windows 用户目录与 web-work 拼成路径，用户名有中文或空格也适用。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：任意目录

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 创建工作文件夹；Directory 表示文件夹，Force 在这里允许复用已有目录。
New-Item -ItemType Directory -Force -Path $workRoot
```

命令解释：创建目录；已存在时复用，-Force 在这里不会清空目录。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：当前目录可以任意；本代码框会切换到指定目录。

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 进入指定文件夹，后面的命令将在这个目录里运行。
Set-Location $workRoot
```

命令解释：进入工作目录。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：本人用户目录下的 web-work

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 保存老师的真实资源仓库地址，直接复制这一行。
$repo = 'https://github.com/02-gdit-ffd-20260818/ffd-student-course-kit.git'
```

命令解释：把真实教学资源仓库地址保存到变量，直接复制，无需修改。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：本人用户目录下的 web-work

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 从远程下载指定仓库或课次分支；最后一个参数是电脑上的保存文件夹。
git clone --single-branch --depth 1 --branch p1-guide-01-v1.2 $repo kit-01-v12
```

命令解释：clone 下载仓库；--branch 指定第 1 课分支；--single-branch 只取该分支；--depth 1 只取最新提交；最后一项是保存目录名。

看到这些再继续：出现下载完成提示，web-work 内新增 kit-01-v12。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：本人用户目录下的 web-work

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 检查路径是否存在，存在输出 True，不存在输出 False。
Test-Path './kit-01-v12/starter/package.json'
```

命令解释：确认模板内有工程入口文件。

看到这些再继续：True。False 表示领取失败或当前目录不对。

若提示 destination path already exists：不要重复 clone，也不要删除文件。先核对现有 kit-01-v12 的 README；确实是本课资源时，直接使用它。需要更新资料时，先核对当前确实是领取目录，再按老师的资料更新说明操作；个人开发工程继续保留。

若网络暂时无法访问 GitHub：让老师发送“P1_第01课_学生资源_v1.2.zip”。在资源管理器中右键 ZIP，选择“全部解压缩”；把解压后的 kit-01-v12 文件夹放到本人用户目录的 web-work 内。不要在压缩包窗口中直接编辑文件。确认路径恰好为 web-work/kit-01-v12/starter/package.json，不能多套一层同名文件夹。随后继续步骤 3。离线 ZIP 可以领取模板，首次依赖安装与远程推送仍需要网络。

## 3. 准备自己的开发工程

位置：刚才的 Windows PowerShell，web-work 目录。只在第一次开始项目时复制模板。

运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：本人用户目录下的 web-work

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 设置个人开发目录 p1-portfolio，与 kit 领取目录并列。
$project = Join-Path $workRoot 'p1-portfolio'
```

命令解释：设置自己长期开发的目录名。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：本人用户目录下的 web-work

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 检查前提；不满足时停止当前步骤，避免覆盖已有成果。
if (Test-Path -LiteralPath $project) {
  # 显示原因并停止当前代码块；先处理这个原因再往后做。
  throw '个人项目已经存在：停止复制，打开原项目继续。'
} else {
  # 复制模板文件；Recurse 包含子目录；出现 Force 时会替换同名文件。
  Copy-Item -LiteralPath './kit-01-v12/starter' -Destination $project -Recurse
}
```

命令解释：整个代码框一起执行。if 检查目标是否存在；已存在则停止；else 仅在不存在时复制 starter；-Recurse 包含全部子目录。不会复制 kit 外层的 .git。

看到这些再继续：web-work 内同时有 kit-01-v12 和 p1-portfolio，两者并列。已有项目时不再执行复制，直接打开原工程。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：当前目录可以任意；本代码框会切换到指定目录。

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 进入指定文件夹，后面的命令将在这个目录里运行。
Set-Location (Join-Path $env:USERPROFILE 'web-work/p1-portfolio')
```

命令解释：进入自己的 p1-portfolio；每次换终端都可以用这一行重新定位。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 用 VS Code 打开当前文件夹；点号表示当前目录。
code .
```

命令解释：用 VS Code 打开当前目录；点号表示当前文件夹。

看到这些再继续：VS Code 左侧能直接看到 package.json、README.md 和本课代码文件。

若 code 无法识别：手动打开 VS Code，点击“文件 → 打开文件夹”，选择用户目录下 web-work/p1-portfolio，然后点击“选择文件夹”。如出现信任提示，核对确实是刚领取的本课程模板，再选择信任。

## 4. 运行初稿，先使用一个终端

在 VS Code 顶部点击“终端 → 新建终端”。看终端标签，选择 PowerShell；如果默认不是，在终端右上方下拉菜单选择 PowerShell 新建。第一次只使用这个终端。需要输入检查或 Git 命令时，按 Ctrl+C 停止网页，等 PS 提示符重新出现，再输入下一条命令。

运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：当前目录可以任意；本代码框会切换到指定目录。

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 进入指定文件夹，后面的命令将在这个目录里运行。
Set-Location (Join-Path $env:USERPROFILE 'web-work/p1-portfolio')
```

命令解释：明确进入当前工程。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 检查路径是否存在，存在输出 True，不存在输出 False。
Test-Path ./package.json
```

命令解释：确认当前目录是工程根目录。

看到这些再继续：True。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 按照 package-lock.json 安装依赖，等待结束后再输入下一条命令。
npm.cmd ci
```

命令解释：根据 package-lock.json 安装约定版本的依赖。等待命令结束并重新出现 PS 提示符。

看到这些再继续：正常结束；若出现 npm ERR 或非零退出码，先处理错误，不继续运行。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 检查初稿是否齐全；允许课堂 TODO 尚未完成。
npm.cmd run check:starter
```

命令解释：检查初稿文件和运行命令是否齐全；这一步允许课堂 TODO 尚未完成。

看到这些再继续：起步检查通过。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 启动本地网页；持续运行时没有新的 PS 提示符是正常现象。
npm.cmd run dev
```

命令解释：启动网页服务。终端被持续占用是正常现象。

看到这些再继续：显示 Local 地址；复制到浏览器地址栏打开。不要双击 index.html 当作项目运行方式。

第 1—5 课通常是 http://127.0.0.1:5173/；第 6 课以 Vite 输出为准。看到初稿页面就算启动成功；初稿没有完成的功能接下来由自己编写。

保持当前网页服务运行，先进入第 5 步修改代码。直到第 6 步开始检查时，才按 Ctrl+C 停止网页。

后续代码修改都在左侧编辑器完成。每改一处按 Ctrl+S 保存，再回浏览器按 Ctrl+R 刷新；第 1—5 课的服务不会替你自动刷新。关闭运行终端 或按 Ctrl+C 后网页服务会停止，再次使用要重跑 npm.cmd run dev。

## 5. 具体开发：找到原文，再替换代码

通用手势：点击 VS Code 左侧“资源管理器”图标，展开目录，单击指定文件。按 Ctrl+F 搜索给出的标记，按 Esc 关闭搜索框；鼠标选中需要替换的原文，把代码粘贴到编辑区，按 Ctrl+S。不要把代码粘贴进搜索框或终端。

代码示范用于带做。先说明自己预期网页发生什么变化，再输入、保存并观察；完成后至少做一处个人修改，解释修改理由。

### 5.1 先改网页主标题，确认自己编辑的是正确文件

打开 index.html，Ctrl+F 搜索“你好，我是林晓”。只改标签之间的文字，例如把整行改成下面这样。周小禾是示例名，你可以使用自己的公开展示名。

```html
    <h1>你好，我是周小禾</h1>
```

保存并刷新，页面大标题应立即改变。若没变：先看编辑器是否打开 p1-portfolio，文件标签是否还有未保存圆点，再看浏览器是不是 终端显示的 Local 地址。

随后 Ctrl+F 搜索其余“林晓”，逐处修改 title、description 和 footer 中的展示名。只改文字，保留标签与属性引号。联系信息可以继续使用模板中的虚构演示邮箱。

### 5.2 修改 index.html

打开 p1-portfolio/index.html。搜索 TODO-L1，找到下面这段原文。只选中这段，不要选中相邻函数或其他区块。

修改前：

```html
<!-- TODO-L1: 在这里编写 about-title 对应的完整 section -->
```

替换成：

```html
<section aria-labelledby="about-title">
      <h2 id="about-title">关于我</h2>
      <p>我正在学习 HTML、CSS、JavaScript 与软件发布，希望成长为重视用户体验的前端开发者。</p>
    </section>
```

代码解释：section 是一个主题区块，h2 是区块标题。aria-labelledby 的值要与 h2 的 id 完全相同。p 中写自己的介绍。

按 Ctrl+S 保存，回浏览器 Ctrl+R 刷新，再做下方检查。

### 5.3 修改 index.html

打开 p1-portfolio/index.html。搜索 TODO-L1，找到下面这段原文。只选中这段，不要选中相邻函数或其他区块。

修改前：

```html
<!-- TODO-L1: 在这里编写 skills-title 对应的完整 section -->
```

替换成：

```html
<section aria-labelledby="skills-title">
      <h2 id="skills-title">正在掌握</h2>
      <ul class="tag-list">
        <li>语义化 HTML</li>
        <li>响应式 CSS</li>
        <li>Git 与 GitHub</li>
      </ul>
    </section>
```

代码解释：section 是一个主题区块，h2 是区块标题。aria-labelledby 的值要与 h2 的 id 完全相同。ul 是列表，每个 li 是一项技能，至少保留两项。

按 Ctrl+S 保存，回浏览器 Ctrl+R 刷新，再做下方检查。

### 5.4 修改 styles.css

打开 p1-portfolio/styles.css。搜索 section {，在这个样式区块内找到 padding: 24px;。只改这一行，不改其他选择器中的 padding。

修改前：

```css
padding: 24px;
```

替换成：

```css
padding: 28px;
```

代码解释：只替换 section { ... } 中这一行。padding 控制卡片内容与边框的距离，单位 px 是像素。

按 Ctrl+S 保存，回浏览器 Ctrl+R 刷新，再做下方检查。

### 5.6 逐项观察结果

页面出现“关于我”和“正在掌握”两个区块，至少两项技能。卡片内容距离边框比原来稍远。用 Tab 键移动到链接，应能看到黄色焦点框。第一段完成后回 PPT 第 23 页；间距与键盘检查完成后回第 27 页。

### 5.7 写下本次改动

在左侧打开 README.md，按 Ctrl+End 到文件末尾，新起一行追加下面这段。把示例中的说明改成你的实际操作，不要覆盖原有安装和运行说明。

```markdown
## 我的本课记录

展示名：周小禾
我修改的文件：填写实际文件名
修改目的：填写希望用户看到的变化
验证方法：填写自己实际执行的检查
结果：填写页面变化或错误修复情况
```

这一步让下一位同学或未来的自己能理解本次修改。填完 Ctrl+S 保存。

## 6. 检查后再提交

位置：VS Code 下方 PowerShell 终端。先按 Ctrl+C 停止网页，等 PS 提示符出现，再执行检查。

运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：当前目录可以任意；本代码框会切换到指定目录。

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 进入指定文件夹，后面的命令将在这个目录里运行。
Set-Location (Join-Path $env:USERPROFILE 'web-work/p1-portfolio')
```

命令解释：回到个人工程根目录。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 检查本课要自己完成的任务，未完成时会报错。
npm.cmd run check:lesson
```

命令解释：检查本课核心实现；提示哪个区块或函数未完成，就返回对应代码修正。

看到这些再继续：命令成功结束；最后的 Test-Path 应为 True。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 运行项目提供的结构检查。
npm.cmd run check
```

命令解释：执行模板约定的结构检查。

看到这些再继续：命令成功结束；最后的 Test-Path 应为 True。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 运行项目提供的自动测试。
npm.cmd test
```

命令解释：执行已提供的测试。

看到这些再继续：命令成功结束；最后的 Test-Path 应为 True。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 生成 dist 文件夹中的部署文件。
npm.cmd run build
```

命令解释：生成部署文件 dist。

看到这些再继续：命令成功结束；最后的 Test-Path 应为 True。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 检查路径是否存在，存在输出 True，不存在输出 False。
Test-Path ./dist/index.html
```

命令解释：确认构建结果包含网页入口。

看到这些再继续：命令成功结束；最后的 Test-Path 应为 True。

自动检查通过后仍要完成第 5 步的网页观察。不要修改测试代码来消除失败提示。

## 7. 创建自己的远程仓库并补上作品链接

位置：浏览器，登录自己的 GitHub 账号。打开 [创建仓库](https://github.com/new)。

1. Owner 选择自己的账号，Repository name 填 p1-portfolio。
2. Description 可填“我的个人作品门户”。选择 Public，方便同学访问代码及后续 Pages 练习。
3. Add README 关闭；.gitignore 和 License 选 None。因为本地已经有这些文件，这里创建一个空仓库。
4. 点击 Create repository。若自己的同名仓库已存在，先看里面是否已经是此项目；是则继续使用，非此项目则另取清楚的名字，不覆盖旧项目。
5. 在 Quick setup 区域选 HTTPS，复制以 .git 结尾的地址。以后推送到这里。地址应属于你的账号，不能是 02-gdit-ffd-20260818 的教学资源仓库。

回 VS Code 的 index.html，搜索 href="https://github.com/"，把这个属性引号中的网址替换为浏览器里“自己的 p1-portfolio 仓库页面地址”。只换引号内的 URL，保留 a 标签、链接文字和 rel 属性。保存刷新，点击“查看代码仓库”应打开自己的仓库。此时空仓库页面正常，推送后才会出现文件。

网址修改后，在当前终端中重新执行第 6 步检查。然后在当前终端中执行以下 Git 操作。

## 8. 创建本地版本记录

以下 init 和身份配置只在首次建项目时执行。若自己已经在本目录建过仓库，保留它，跳过 init，用 git status 查看当前状态。

运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 首次建立个人 Git 仓库，-b main 指定主分支名称。
git init -b main
```

命令解释：在当前 p1-portfolio 内建立 .git 历史目录，主分支叫 main。

看到这些再继续：初始化成功。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 显示引号内的问题；输入自己的信息后按回车，内容保存到左侧变量。
$displayName = Read-Host "输入你的提交展示名"
```

命令解释：输入自己的署名并按回车；这是提交署名，不是 GitHub 密码。

看到这些再继续：回到 PS 提示符。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 显示引号内的问题；输入自己的信息后按回车，内容保存到左侧变量。
$commitEmail = Read-Host "输入你的Git提交邮箱"
```

命令解释：输入自己的提交邮箱；可以使用 GitHub Settings → Emails 提供的 noreply 地址。

看到这些再继续：回到 PS 提示符。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 设置当前仓库的提交署名，不是登录 GitHub。
git config user.name $displayName
```

命令解释：把展示名写入当前仓库配置。

看到这些再继续：不报错。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 设置当前仓库的提交邮箱。
git config user.email $commitEmail
```

命令解释：把邮箱写入当前仓库配置；这两项配置不会让你自动登录 GitHub。

看到这些再继续：不报错。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 查看未提交修改；short 或 porcelain 为简洁格式。
git status --short
```

命令解释：列出变更：M 表示已修改，?? 表示新文件。

看到这些再继续：只包含预期的课程文件。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 查看逐行修改；出现分页界面时按 q 返回。
git diff
```

命令解释：看已跟踪文件的逐行变化。首次提交的新文件可能不在这里显示，请在编辑器查看。

看到这些再继续：核对内容；如果进入分页显示界面，按 q 返回终端。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 把指定文件放入暂存区；点号表示当前工程内的变更。
git add .
```

命令解释：把当前工程的修改放入暂存区，准备纳入下一次提交。

看到这些再继续：不报错。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 查看已经暂存、即将提交的文件摘要。
git diff --cached --stat
```

命令解释：查看即将提交的文件摘要。

看到这些再继续：应包含代码和文档；不应包含 node_modules、dist、账号口令。若出现这些，先停止提交并检查 .gitignore。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 把暂存内容保存为本地提交；-m 后面填写本次修改说明。
git commit -m "feat: complete P1 lesson 01"
```

命令解释：创建本地提交；-m 后面是提交说明。

看到这些再继续：输出提交号和文件变更；nothing to commit 时先看是否已经提交。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 查看提交历史；oneline 为简洁格式，graph 显示分支形状。
git log -1 --oneline
```

命令解释：显示最近一条本地提交号与说明。

看到这些再继续：记录此提交号，稍后与 GitHub 核对。

## 9. 连接并首次推送到自己的 GitHub

位置：当前 PowerShell 终端，p1-portfolio。

运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 显示引号内的问题；输入自己的信息后按回车，内容保存到左侧变量。
$remoteUrl = Read-Host "粘贴刚才复制的个人仓库HTTPS地址"
```

命令解释：粘贴自己的 .git 地址再回车。不要把说明中的汉字当成地址。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 首次把自己的远程仓库地址命名为 origin。
git remote add origin $remoteUrl
```

命令解释：把这个远程地址命名为 origin。只添加一次；已存在时先执行下一条核对。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 显示远程地址，必须核对是否属于自己的成果仓库。
git remote -v
```

命令解释：显示 fetch 和 push 地址。

看到这些再继续：两个地址都属于自己的账号和本项目。若错误，用附录中的“远程地址错误”处理后再推送。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 把本地提交上传到自己的远程；-u 用于首次建立分支跟踪关系。
git push -u origin main
```

命令解释：push 上传本地提交；origin 是远程仓库；main 是本次上传分支；-u 记住对应关系。

看到这些再继续：首次可能弹出登录窗口，按下一段完成认证。已有凭据时可能直接成功。

若弹出 Git Credential Manager：选择“Sign in with your browser”，在浏览器核对自己的账号，完成登录和页面要求的授权或二次验证，再回终端等待推送完成。凭据由 Git Credential Manager 管理，不写进命令或代码。若终端直接索要 Password，不输入 GitHub 登录密码反复尝试；先按 Ctrl+C，核对 Git for Windows 是否安装了 Credential Manager，再按附录排查。

成功输出可能包含 “[new branch] main -> main” 或 “main -> main”。仅看见本地 commit 的提示不等于 push 完成。推送成功后继续第 10 步核对。

## 10. 在远程网页确认成果确实上传

位置：浏览器，打开自己的 GitHub 仓库，按 Ctrl+R 刷新，点击 Code，分支选择 main。

1. 根目录应直接显示 package.json 和本课代码文件，不能只有一个 starter 文件夹。
2. 点击本课修改的文件，看到刚才写入的新内容。
3. 点击文件列表上方的最近提交，核对说明和提交号是否与终端 git log -1 --oneline 一致。

在当前 PowerShell 终端 也可以核对完整提交号：

运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 显示当前本地提交的完整编号。
git rev-parse HEAD
```

命令解释：显示本地当前提交的完整编号。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 查询远程指定分支的提交编号，用于和本地核对。
git ls-remote origin refs/heads/main
```

命令解释：查询自己的远程 main 提交号；第一列应与上一条编号完全相同。

看到这些再继续：两个编号相同，加上网页文件内容正确，才算本轮推送完成。

## 11. 再修改一次，练习日常提交

现在不用重新下载模板、建仓库或设置 origin。打开 README.md，在“我的本课记录”末尾添加一句“我已完成第二次修改并核对远程提交”，Ctrl+S 保存。

以下命令都在当前终端中依次执行：

运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 查看双横线后指定文件的变化。
git diff -- README.md
```

命令解释：只查看 README.md 的变化，双横线后面是文件路径。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 把指定文件放入暂存区；点号表示当前工程内的变更。
git add README.md
```

命令解释：只暂存这次改动的 README 文件。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 把暂存内容保存为本地提交；-m 后面填写本次修改说明。
git commit -m "docs: record second classroom update"
```

命令解释：为第二次修改创建新记录。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 把本地提交上传到自己的远程；-u 用于首次建立分支跟踪关系。
git push origin main
```

命令解释：把新记录继续上传到同一个远程主分支。

刷新 GitHub 的 README 页面，应看到追加文字和新提交号。如果修改的是代码而非文档，应先重跑第 6 步相应检查，再 add、commit、push。

## 12. 把代码发布成可访问的网站

代码推送成功后，浏览器打开自己的仓库，选择 Settings → Pages → Build and deployment，把 Source 设为 GitHub Actions。随后点击 Actions，打开 test-and-deploy-pages 的最新一次运行。

模板自带 .github/workflows/deploy-pages.yml，不需要另写发布脚本。verify 和 deploy 都成功后，打开部署显示的 URL。若第一次推送早于 Pages 设置而导致失败，完成设置后在该运行页选择 Re-run jobs → Re-run all jobs；也可以进入该工作流选择 Run workflow，分支选 main。

用另一个浏览器未登录窗口打开部署 URL，检查本课内容。本机 127.0.0.1 地址不能发给同学当成公开网址。GitHub 的仓库代码页面和 Pages 网站是两个地址，提交作业时分别填写。

Ubuntu 部署放在第六课 PPT 第 45—58 页的独立附录中，本手册先完成 Windows 开发和 GitHub 推送。P1 是静态作品门户，数据库仍按后续项目学习安排推进。

## 13. 下课交付与下次继续

向老师提交：自己的仓库链接、最近提交号、网页效果截图、公开网址（发布课必须有），以及一句“本次修改为什么有效”的解释。老师抽查时，让学生现场定位一个修改文件，说明自己改变了什么。

下次还是同一台电脑：打开 VS Code 中的 p1-portfolio，打开一个 PowerShell 终端，运行网页；需要检查和 Git 操作时先 Ctrl+C 停止网页。不重做第一课的 init 和 remote add。换到下一课，使用下一课手册的备份、导入和个人内容恢复步骤。

如果换了电脑：先安装工具，在新的 web-work 下，用自己的 GitHub 仓库 HTTPS 地址执行 git clone，目标目录叫 p1-portfolio；不要用老师模板代替自己的已有成果。然后 npm.cmd ci，再 npm.cmd run dev。具体命令如下，只在新电脑且目标目录不存在时执行：

运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：当前目录可以任意；本代码框会切换到指定目录。

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 确定工作目录：本人 Windows 用户文件夹下的 web-work。
$workRoot = Join-Path $env:USERPROFILE 'web-work'
# 创建工作文件夹；Directory 表示文件夹，Force 在这里允许复用已有目录。
New-Item -ItemType Directory -Force -Path $workRoot
# 进入指定文件夹，后面的命令将在这个目录里运行。
Set-Location $workRoot
```

命令解释：准备新电脑的工作目录。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：本人用户目录下的 web-work

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 显示引号内的问题；输入自己的信息后按回车，内容保存到左侧变量。
$mine = Read-Host "粘贴自己的成果仓库HTTPS地址"
```

命令解释：输入自己的 p1-portfolio 地址。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：本人用户目录下的 web-work

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 从远程下载指定仓库或课次分支；最后一个参数是电脑上的保存文件夹。
git clone $mine p1-portfolio
```

命令解释：下载自己已推送的历史和代码；目标已存在时停止，不覆盖。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：当前目录可以任意；本代码框会切换到指定目录。

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 进入指定文件夹，后面的命令将在这个目录里运行。
Set-Location ./p1-portfolio
# 按照 package-lock.json 安装依赖，等待结束后再输入下一条命令。
npm.cmd ci
```

命令解释：进入取回的工程，恢复依赖；安装成功后再启动服务。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 启动本地网页；持续运行时没有新的 PS 提示符是正常现象。
npm.cmd run dev
```

命令解释：运行自己的最新成果。

## 附录：卡住时按现象处理

### 找不到 package.json，或 npm 提示 ENOENT

你可能在 kit 外层、starter 外层或其他目录。执行下面命令，看到 True 后再继续：

运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：当前目录可以任意；本代码框会切换到指定目录。

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 进入指定文件夹，后面的命令将在这个目录里运行。
Set-Location (Join-Path $env:USERPROFILE 'web-work/p1-portfolio')
# 检查路径是否存在，存在输出 True，不存在输出 False。
Test-Path ./package.json
```

命令解释：重新进入个人工程并检查入口文件。

### 浏览器没有更新

先 Ctrl+S 保存，确认文件标签没有未保存圆点；再 Ctrl+R 刷新。确认地址是 终端显示的 Local 地址而不是远程 Pages。若 运行终端已关闭，重跑 npm.cmd run dev；如果提示 EADDRINUSE，找到自己之前的运行终端 Ctrl+C 停止，不随意结束其他程序。

### 红色错误太长，不知道看哪条

先找第一条 Error、AssertionError 或 npm ERR，把报错命令、当前路径和第一条错误交给老师。TODO 尚未完成时 check:lesson 失败是正常的。不要把整份工程替换为答案来掩盖错误。

### fatal: not a git repository

先确认当前目录以 p1-portfolio 结尾。首次尚未执行第 8 步时需要 git init；已经做过项目却出现此提示，先检查是否打开了 kit 或另一个文件夹，不到处重复 init。

### Author identity unknown

在当前个人仓库补做第一课第 8 步的 Read-Host 与 git config 两项身份设置，再重试 commit。user.name 与 user.email 是提交署名，不能代替远程登录。

### remote origin already exists，或者远程地址填错

先 git remote -v。地址正确就跳过 remote add。只有确认当前是自己的 p1-portfolio、但 origin 明确填错时，才执行下面修正：

运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 显示引号内的问题；输入自己的信息后按回车，内容保存到左侧变量。
$correctUrl = Read-Host "粘贴核对过的个人仓库HTTPS地址"
```

命令解释：重新输入自己的成果仓库地址。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 只在确认 origin 地址填错时修正，不改变本地历史。
git remote set-url origin $correctUrl
```

命令解释：修正当前仓库 origin 的地址，不更改提交历史。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 显示远程地址，必须核对是否属于自己的成果仓库。
git remote -v
```

命令解释：再次核对地址，然后重试 push。

### 推送显示 403、Permission denied 或 Repository not found

先在浏览器打开 origin 的地址，确认仓库存在且属于你，再检查 Git Credential Manager 登录的账号是否相同。共享机可能保存了别人的凭据，请老师协助切换到本人账号，不要用别人账号继续提交。若没弹出浏览器认证窗口，可运行下面两条诊断，把结果交给老师核对 Git for Windows 安装：

运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 检查 Git Credential Manager 是否安装。
git credential-manager --version
```

命令解释：确认 Git Credential Manager 可用。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 显示凭据助手的配置来源，不显示密码。
git config --show-origin --get-all credential.helper
```

命令解释：查看凭据助手配置来源，不显示密码；没有输出时让老师检查 Git 安装选项。

### 推送显示 rejected / non-fast-forward

常见原因是在 GitHub 网页又改过文件、换电脑产生了不同提交，或者创建远程仓库时添加了 README。不要 force push。先执行以下只读诊断，把历史交给老师选择合并方式：

运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 取回远程历史用于比较，不覆盖当前工作文件。
git fetch origin
```

命令解释：取回远程历史供比较，不覆盖当前工作文件。


运行平台：Windows 10/11 · PowerShell。输入位置：VS Code 下方的 PowerShell 终端。

当前目录：web-work/p1-portfolio（自己的开发工程）

运行方法：点击终端最后一行，粘贴本代码框并按 Enter；# 开头是注释，可以一起复制。看到预期结果后再做下一步。

```powershell
# 查看提交历史；oneline 为简洁格式，graph 显示分支形状。
git log --oneline --graph --all -12
```

命令解释：显示最近历史和分支形状；如有分页显示，按 q 退出。

### 推送后提示 Everything up-to-date，但网页没有新内容

检查是否只是 Ctrl+S 保存了文件，却没有 commit；git status --short 有修改时，继续 add、commit、push。若代码页已经更新而网站未更新，检查对应提交的 Actions 部署状态。

### 不小心覆盖了上一课自己的介绍

打开 web-work/p1-before-课次.zip 中的旧文件，只复制自己的文字和数据回新工程。不要用旧工程整包覆盖当前项目。备份是已提交文件的快照，未提交内容不会自动出现在备份里。

## 操作依据

- [GitHub：将本地代码添加到远程仓库](https://docs.github.com/en/migrations/importing-source-code/using-the-command-line-to-import-source-code/adding-locally-hosted-code-to-github)
- [GitHub：Windows 凭据缓存与浏览器认证](https://docs.github.com/en/get-started/git-basics/caching-your-github-credentials-in-git)
- [GitHub：Pages 自定义工作流](https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages)

