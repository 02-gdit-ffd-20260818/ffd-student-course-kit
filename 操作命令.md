# 第01课 名片上线：操作命令逐条解释 v1.2

与本课43页PPT一一对应。按页序执行，单条命令失败时停止后续依赖步骤。用户工作目录为USERPROFILE下的web-work。

## PPT第1页：一页知我 · 第 01 课

名片上线
版本 v1.0
完成自己的语义化名片，提交 Git 版本，并获得可访问的网址

下一步：完成本页后继续第2页

## PPT第2页：本课顺序与时间安排

0—10：看需求、查旧知。
10—25：讲解与最小示范。
25—50：第一段操作；50—60：针对卡点补讲。
60—100：第二段操作；100—120：测试与提交。
120—135：展示、解释与复盘。

下一步：完成本页后继续第3页

## PPT第3页：先识别当前操作环境

PS C:\…>：本机 Windows PowerShell。
本课命令统一在 PowerShell 中运行。
npm.cmd 与 npm 执行同一套npm命令。
Ubuntu 操作仅在第6课附录明确标注。

下一步：完成本页后继续第4页

## PPT第4页：课前安装与打开终端

Git：https://git-scm.com/downloads/win
Node.js：https://nodejs.org/en/download
VS Code：https://code.visualstudio.com/Download
安装 Node 24，安装后重开编辑器和 PowerShell。

下一步：完成本页后继续第5页

## PPT第5页：确认工具可用

环境：**Windows PowerShell**。位置：**任意目录**。

```powershell
git --version
```

解释：显示Git版本，确认可以领取和提交代码。

```powershell
node --version
npm.cmd --version
```

解释：分别显示Node与npm版本；Node按本课使用v24。

成功标志：出现版本号。

遇到问题：无法识别：安装工具后重开终端；不要继续clone或安装依赖。

下一步：完成本页后继续第6页

## PPT第6页：本课资源的准确地址

https://github.com/02-gdit-ffd-20260818/ffd-student-course-kit
本课分支：p1-guide-01-v1.2
点击仓库分支选择器，选中上述分支。
下一页开始领取；也可在Code菜单下载本分支ZIP。

下一步：完成本页后继续第7页

## PPT第7页：建立统一工作目录

环境：**Windows PowerShell**。位置：**任意目录**。

```powershell
$workRoot = Join-Path $env:USERPROFILE 'web-work'
```

解释：Join-Path拼接当前用户目录与web-work，适用不同用户名。

```powershell
New-Item -ItemType Directory -Force $workRoot
Set-Location $workRoot
```

解释：建立或复用工作目录，再进入它。-Force在此不会清空目录。

成功标志：终端位于用户目录下的web-work。

遇到问题：目录不可写：改用本人有权限的目录，后续保持一致。

下一步：完成本页后继续第8页

## PPT第8页：指定本课领取来源

环境：**Windows PowerShell**。位置：**web-work**。

```powershell
$repo = "https://github.com/02-gdit-ffd-20260818/"
$repo += "ffd-student-course-kit.git"
```

解释：两行拼成完整仓库地址。+=表示把后半段追加到变量。

```powershell
$branch = "p1-guide-01-v1.2"
$kit = "kit-01-v12"
```

解释：branch是本课分支，kit是本次领取目录；v12用于区别旧资料。

成功标志：三个变量已设置，未报错即可继续。

遇到问题：关闭过终端：从工作目录页重做变量设置。

下一步：完成本页后继续第9页

## PPT第9页：领取并核对资源

环境：**Windows PowerShell**。位置：**web-work**。

```powershell
git clone --single-branch --depth 1 `
  --branch $branch $repo $kit
```

解释：clone下载；--branch指定课次；--single-branch只取该分支；--depth 1只取最新提交。反引号续行，后面不能有空格。

```powershell
Get-ChildItem -LiteralPath $kit
```

解释：列出领取目录内容，核对README、操作命令和starter。

成功标志：本课目录出现，starter内有package.json。

遇到问题：目录已存在：打开README核对；不要删除学生成果或强行覆盖。

下一步：完成本页后继续第10页

## PPT第10页：建立自己的项目副本

环境：**Windows PowerShell**。位置：**web-work**。

```powershell
$project = Join-Path $workRoot 'p1-portfolio'
```

解释：project保存自己的长期开发目录，与领取目录并列。

```powershell
if (Test-Path $project) { throw "项目已存在，请先检查" }
```

解释：Test-Path判断存在性；throw停止当前命令。已有项目时停止本次复制，改为打开原工程。

成功标志：目录不存在，可以进行下一页复制。

遇到问题：看到“项目已存在”：不要执行下一页Copy-Item。

下一步：完成本页后继续第11页

## PPT第11页：复制起步文件并打开

环境：**Windows PowerShell**。位置：**web-work**。

```powershell
Copy-Item -LiteralPath "$kit/starter" `
  -Destination $project -Recurse
```

解释：把starter及其子目录复制为个人工程；不会复制领取仓库的.git。

```powershell
Set-Location $project
code .
```

解释：进入个人工程；code .用VS Code打开当前目录。

成功标志：编辑器根目录含index.html与package.json。

遇到问题：code不可用：用编辑器“文件→打开文件夹”打开p1-portfolio。

下一步：完成本页后继续第12页

## PPT第12页：确认终端目录与安装依赖

环境：**Windows PowerShell**。位置：**编辑器中新建PowerShell终端A**。

```powershell
$workRoot = Join-Path $env:USERPROFILE 'web-work'
Set-Location (Join-Path $workRoot 'p1-portfolio')
```

解释：即使新终端没有上页变量，也能准确进入个人工程。

```powershell
Get-Location
Test-Path ./package.json
```

解释：显示当前路径，并确认当前目录存在package.json。

成功标志：路径以p1-portfolio结尾，最后显示True。

遇到问题：False：回到项目根目录；不要在kit目录运行npm。

下一步：完成本页后继续第13页

## PPT第13页：安装并检查起步条件

环境：**Windows PowerShell**。位置：**p1-portfolio；PowerShell终端A**。

```powershell
npm.cmd ci
```

解释：按package-lock.json安装依赖，保证全班版本一致；需可用网络。

```powershell
npm.cmd run check:starter
```

解释：检查课堂文件与命令是否齐备，不检查TODO是否完成。

成功标志：依赖安装结束，起步检查通过。

遇到问题：ci失败：核对网络与锁文件；保留第一条错误，不自行升级依赖。

下一步：完成本页后继续第14页

## PPT第14页：启动网页并保持服务运行

环境：**Windows PowerShell**。位置：**p1-portfolio；PowerShell终端A**。

```powershell
npm.cmd run dev
```

解释：启动本地开发服务；运行期间这个终端会一直被占用。

解释：用浏览器打开终端显示的Local地址；第1—5课通常为http://127.0.0.1:5173/。

成功标志：页面能打开，看到本课起点。

遇到问题：端口占用：在旧服务终端按Ctrl+C后重启；第6课以Vite实际地址为准。

下一步：完成本页后继续第15页

## PPT第15页：接下来修改哪里

文件：index.html, styles.css, README.md
搜索标记：TODO-L1
保存快捷键：Ctrl+S。浏览器检查实际结果。
实操结束按页脚返回检查页。

下一步：完成本页后继续第16页

## PPT第16页：第一次发布的用户需求

访问者需要快速知道：你是谁、会什么、作品在哪里。
本次先完成清楚可读的名片，视觉细节随后迭代。
验收：两个新区块、可用作品链接、个人提交、公开网址。

下一步：完成本页后继续第17页

## PPT第17页：先检查 HTML 与文件基础

题 1：一个页面的主标题用什么标签？
题 2：样式应该修改哪个文件？
题 3：保存文件后，怎样看到最新效果？

下一步：完成本页后继续第18页

## PPT第18页：今天只打开三个文件

index.html：页面内容与结构。
styles.css：颜色、间距和排版。
README.md：写给接手者看的启动说明。

下一步：完成本页后继续第19页

## PPT第19页：语义标签表达内容的角色

header：页面介绍。
main：页面的主要内容。
section：围绕一个主题组织内容。
h1、h2：主标题与区块标题。

下一步：完成本页后继续第20页

## PPT第20页：一个完整的关于我区块

section 与其标题通过 aria-labelledby、id 对应。
h2 表示这个区块的标题，p 表示说明。
本页代码替换 about-title 对应的 TODO 注释。

```text
<section aria-labelledby="about-title">
  <h2 id="about-title">关于我</h2>
  <p>我正在学习前端开发，喜欢把信息整理清楚。</p>
</section>
```

下一步：完成本页后继续第21页

## PPT第21页：技能列表的结构

一个技能作为一个 li。
ul 组织多项并列技能。
类名 tag-list 复用已有样式。

```text
<section aria-labelledby="skills-title">
  <h2 id="skills-title">正在掌握</h2>
  <ul class="tag-list">
    <li>语义化 HTML</li>
    <li>响应式 CSS</li>
  </ul>
</section>
```

下一步：完成本页后继续第22页

## PPT第22页：第一段操作：完成个人内容

打开：index.html
定位：TODO-L1
修改或操作：补齐两个section、标题和技能列表。
成功标志：浏览器显示自己的介绍和技能。
完成后回到第23页检查。

下一步：完成后回到第23页检查

## PPT第23页：回到PPT：第1段检查

浏览器显示自己的介绍和技能。
展示结果，并指出对应的代码或操作。
未达到：返回前一页修正，再进入下一步。

下一步：完成本页后继续第24页

## PPT第24页：检查点：结构与显示一致

每人指出自己的 main、h2 和 li。
同桌能在十秒内找到技能和作品入口。
如果区块不出现，检查标签闭合与文件是否保存。

下一步：完成本页后继续第25页

## PPT第25页：CSS 盒模型决定间距

content：内容区域。
padding：内容到边框之间的距离。
border：边框。
margin：区块与外部元素的距离。

```text
section {
  margin: 18px 0;
  padding: 24px;
  border: 1px solid #cad6e4;
}
```

下一步：完成本页后继续第26页

## PPT第26页：第二段操作：可读与可访问

打开：styles.css、index.html
定位：section样式与作品链接
修改或操作：调整一处间距，检查链接与键盘焦点。
成功标志：文字可读，链接可访问，焦点可见。
完成后回到第27页检查。

下一步：完成后回到第27页检查

## PPT第27页：回到PPT：第2段检查

文字可读，链接可访问，焦点可见。
展示结果，并指出对应的代码或操作。
未达到：返回前一页修正，再进入下一步。

下一步：完成本页后继续第28页

## PPT第28页：排错先找发生在哪一层

本地不显示：先检查 HTML、路径和保存。
Git 无法提交：看身份或仓库提示。
Actions 红色：打开第一个失败步骤。
网站未更新：确认部署的 commit。

下一步：完成本页后继续第29页

## PPT第29页：个人变式任务

把“正在掌握”改成“能为团队做什么”，保留正确标题层级和列表结构。
先写预期，再动手修改，最后说明依据。

下一步：完成本页后继续第30页

## PPT第30页：新开检查终端，确认核心任务

环境：**Windows PowerShell**。位置：**p1-portfolio；PowerShell终端B**。

```powershell
$workRoot = Join-Path $env:USERPROFILE 'web-work'
Set-Location (Join-Path $workRoot 'p1-portfolio')
```

解释：终端A继续运行网页，终端B用于检查和Git操作。

```powershell
npm.cmd run check:lesson
```

解释：运行本课核心验收；TODO未完成时失败属于预期。

成功标志：显示本课验收通过。

遇到问题：失败：根据提示返回对应TODO修正，不直接改测试。

下一步：完成本页后继续第31页

## PPT第31页：结构检查与自动测试

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
npm.cmd run check
```

解释：检查本课页面或Vue结构约定。

```powershell
npm.cmd test
```

解释：执行已经提供的功能测试；检查正常、边界和失败情形。

成功标志：两项命令都成功结束。

遇到问题：只修复第一处相关错误后复测；不要把失败输出当作通过。

下一步：完成本页后继续第32页

## PPT第32页：生成并检查部署文件

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
npm.cmd run build
Test-Path ./dist/index.html
```

解释：build生成dist；Test-Path确认入口文件存在。

```powershell
Get-ChildItem ./dist
```

解释：列出构建产物，确认有index.html及页面需要的资源文件。

成功标志：构建成功并显示True，dist中有入口文件。

遇到问题：构建失败先检查第一条错误，不能上传不完整的dist。

下一步：完成本页后继续第33页

## PPT第33页：首次初始化个人Git仓库

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
git init -b main
```

解释：在当前个人工程建立Git仓库，初始分支命名main。

```powershell
$displayName = Read-Host "输入提交显示名"
$commitEmail = Read-Host "输入Git提交邮箱"
```

解释：Read-Host主动询问信息，输入自己的显示名和邮箱；可使用GitHub noreply邮箱。

成功标志：出现仓库初始化提示，两个输入完成。

遇到问题：已有个人仓库：不重复初始化，先git branch检查分支。

下一步：完成本页后继续第34页

## PPT第34页：设置本仓库提交身份

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
git config user.name $displayName
git config user.email $commitEmail
```

解释：设置本仓库的提交署名；不修改其他仓库身份，也不是GitHub登录。

```powershell
git config --get user.name
git config --get user.email
```

解释：读取刚设置的值，确认与本人填写内容一致。

成功标志：显示正确的姓名与邮箱。

遇到问题：变量为空：返回上一页重新输入；不要将密码当作邮箱。

下一步：完成本页后继续第35页

## PPT第35页：检查并暂存本课成果

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
git status --short
git diff
```

解释：查看未提交文件与已跟踪内容的变化；新文件在编辑器核对。

```powershell
git add .
git diff --cached --stat
```

解释：暂存本工程的改动，再概览将提交的文件。确认没有node_modules、dist或凭据。

成功标志：暂存列表只包含应提交的代码、配置和课堂说明。

遇到问题：nothing to commit：检查是否已提交；不要为了次数制造空提交。

下一步：完成本页后继续第36页

## PPT第36页：创建本地版本记录

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
git commit -m "feat: complete P1 lesson 01"
```

解释：commit创建本地记录；-m后面是本次修改说明。

```powershell
git status --short
git log -1 --oneline
```

解释：确认工作区干净，并显示刚生成的提交号。

成功标志：status无输出，log出现本课提交。

遇到问题：nothing to commit通常表示已经提交；先检查历史。

下一步：完成本页后继续第37页

## PPT第37页：在GitHub创建个人仓库

浏览器打开 https://github.com/new 并登录。
Repository name填写 p1-portfolio，选择Public。
README、.gitignore、License均暂不勾选。
点击Create repository，复制HTTPS仓库地址。

下一步：完成本页后继续第38页

## PPT第38页：连接自己的远程仓库

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
$remoteUrl = Read-Host "粘贴自己的HTTPS仓库地址"
```

解释：输入个人仓库页面的地址，以.git结尾；不要粘贴学生资源仓库地址。

```powershell
git remote add origin $remoteUrl
git remote -v
```

解释：origin是远程仓库别名；添加后列出地址，确认指向自己的p1-portfolio。

成功标志：fetch与push地址都属于自己的仓库。

遇到问题：origin已存在：先核对地址，不能重复add或盲目改地址。

下一步：完成本页后继续第39页

## PPT第39页：首次推送个人主分支

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
git push -u origin main
```

解释：把main推到origin；-u建立跟踪关系，后续可识别对应远程分支。

```powershell
git log -1 --oneline
```

解释：显示本地最新提交号，稍后与GitHub页面和Actions核对。

成功标志：远程仓库Code页面出现个人文件。

遇到问题：按Git Credential Manager提示登录；push被拒绝时先看账号和远程地址。

下一步：完成本页后继续第40页

## PPT第40页：在网页启用或确认Pages

打开自己的GitHub仓库，点击Settings。
左侧选择Pages。
Build and deployment的Source选GitHub Actions。
这是网页操作，无需在终端输入菜单文字。

下一步：完成本页后继续第41页

## PPT第41页：检查自动构建与部署

点击仓库Actions，打开最新test-and-deploy-pages。
核对它对应刚才的commit。
verify与deploy都成功后，打开部署显示的URL。
设置Pages前首次运行失败时，配置后重跑任务。

下一步：完成本页后继续第42页

## PPT第42页：检查公开网址与完成本课

用另一个设备或未登录窗口打开部署URL。
检查个人文字、作品链接和本课新增功能。
提交公开URL、个人commit与离堂题回答。
普通课可先收本地成果，发布里程碑完整验收。

下一步：完成本页后继续第43页

## PPT第43页：离堂检查与下一步

概念题：HTML 的 main 和 section 分别表达什么？
应用题：commit 之后，网站一定已经更新了吗？
把自己的一个错误写成“预期、实际、修复”。

下一步：本课完成

## 官方操作依据

- [GitHub Pages工作流](https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages)
- [Windows OpenSSH](https://learn.microsoft.com/en-us/windows-server/administration/openssh/openssh-overview)
- [Ubuntu Nginx](https://ubuntu.com/server/docs/how-to/web-services/install-nginx/)
