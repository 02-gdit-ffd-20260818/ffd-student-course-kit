# 第05课 主题记忆与请求：操作命令逐条解释 v1.2

与本课45页PPT一一对应。按页序执行，单条命令失败时停止后续依赖步骤。用户工作目录为USERPROFILE下的web-work。

## PPT第1页：一页知我 · 第 05 课

主题记忆与请求
版本 v1.2.2
实现主题切换与刷新保持，验证项目数据请求的正常、空和失败状态

下一步：完成本页后继续第2页

## PPT第2页：本课顺序与时间安排

0—10：看需求、查旧知。
10—25：讲解与最小示范。
25—50：第一段操作；50—60：针对卡点补讲。
60—100：第二段操作；100—120：测试与提交。
120—135：展示、解释与复盘。

下一步：完成本页后继续第3页

## PPT第3页：先识别当前操作环境

PS C:\…>：本机 Windows PowerShell。
本课命令统一在 PowerShell 中运行。
npm.cmd 与 npm 执行同一套npm命令。
Ubuntu 操作仅在第6课附录明确标注。

下一步：完成本页后继续第4页

## PPT第4页：课前安装与打开终端

Git：https://git-scm.com/downloads/win
Node.js：https://nodejs.org/en/download
VS Code：https://code.visualstudio.com/Download
安装 Node 24，安装后重开编辑器和 PowerShell。

下一步：完成本页后继续第5页

## PPT第5页：确认工具可用

环境：**Windows PowerShell**。位置：**任意目录**。

```powershell
git --version
```

解释：显示Git版本，确认可以领取和提交代码。

```powershell
node --version
npm.cmd --version
```

解释：分别显示Node与npm版本；Node按本课使用v24。

成功标志：出现版本号。

遇到问题：无法识别：安装工具后重开终端；不要继续clone或安装依赖。

下一步：完成本页后继续第6页

## PPT第6页：本课资源的准确地址

https://github.com/02-gdit-ffd-20260818/ffd-student-course-kit
本课分支：p1-guide-05-v1.2
点击仓库分支选择器，选中上述分支。
下一页开始领取；也可在Code菜单下载本分支ZIP。

下一步：完成本页后继续第7页

## PPT第7页：建立统一工作目录

环境：**Windows PowerShell**。位置：**任意目录**。

```powershell
$workRoot = Join-Path $env:USERPROFILE 'web-work'
```

解释：Join-Path拼接当前用户目录与web-work，适用不同用户名。

```powershell
New-Item -ItemType Directory -Force $workRoot
Set-Location $workRoot
```

解释：建立或复用工作目录，再进入它。-Force在此不会清空目录。

成功标志：终端位于用户目录下的web-work。

遇到问题：目录不可写：改用本人有权限的目录，后续保持一致。

下一步：完成本页后继续第8页

## PPT第8页：指定本课领取来源

环境：**Windows PowerShell**。位置：**web-work**。

```powershell
$repo = "https://github.com/02-gdit-ffd-20260818/"
$repo += "ffd-student-course-kit.git"
```

解释：两行拼成完整仓库地址。+=表示把后半段追加到变量。

```powershell
$branch = "p1-guide-05-v1.2"
$kit = "kit-05-v12"
```

解释：branch是本课分支，kit是本次领取目录；v12用于区别旧资料。

成功标志：三个变量已设置，未报错即可继续。

遇到问题：关闭过终端：从工作目录页重做变量设置。

下一步：完成本页后继续第9页

## PPT第9页：领取并核对资源

环境：**Windows PowerShell**。位置：**web-work**。

```powershell
git clone --single-branch --depth 1 `
  --branch $branch $repo $kit
```

解释：clone下载；--branch指定课次；--single-branch只取该分支；--depth 1只取最新提交。反引号续行，后面不能有空格。

```powershell
Get-ChildItem -LiteralPath $kit
```

解释：列出领取目录内容，核对README、操作命令和starter。

成功标志：本课目录出现，starter内有package.json。

遇到问题：目录已存在：打开README核对；不要删除学生成果或强行覆盖。

下一步：完成本页后继续第10页

## PPT第10页：继续个人工程，检查上一课记录

环境：**Windows PowerShell**。位置：**web-work**。

```powershell
Set-Location (Join-Path $workRoot 'p1-portfolio')
```

解释：进入上一次的个人工程；保留同一个.git与远程仓库。

```powershell
git status --short
git log -1 --oneline
```

解释：status查看未提交改动，log显示最近一次提交。

成功标志：status没有输出，log显示上一课成果提交。

遇到问题：有改动：先保存、检查并按本课后面的提交步骤提交，再升级。

下一步：完成本页后继续第11页

## PPT第11页：备份并建立本课分支

环境：**Windows PowerShell**。位置：**p1-portfolio**。

```powershell
git archive -o ../p1-before-05.zip HEAD
```

解释：把上次已提交的全部项目文件打成ZIP，保存在工程外；不含未提交文件。

```powershell
git switch main
git switch -c lesson-05
```

解释：切到主线，再新建本课开发分支。上一课已合并main后才执行。

成功标志：备份ZIP存在，分支为lesson-05。

遇到问题：分支已存在：用git branch检查，再git switch切到已有本课分支。

下一步：完成本页后继续第12页

## PPT第12页：确认干净后引入本课骨架

环境：**Windows PowerShell**。位置：**p1-portfolio**。

```powershell
if (git status --porcelain) { throw "先提交所有改动" }
```

解释：存在未提交内容则停止，本页后续复制不执行。备份上一课是必要前提。

```powershell
$source = "../kit-05-v12/starter"
Get-ChildItem -LiteralPath $source -Force |
  Copy-Item -Destination . -Recurse -Force
```

解释：复制本课文件，替换同名教学骨架。starter不含.git，个人Git历史继续保留。

成功标志：本课TODO与检查脚本已进入个人工程。

遇到问题：找不到source：核对领取目录；切勿改成教师答案目录。

下一步：完成本页后继续第13页

## PPT第13页：迁回自己的内容，再开始本课

打开 ../p1-before-05.zip 查看上一课文件。
本课：按下一行保留个人作品与页面文字。
个人作品同时写入projects.json与回退data.js。
自己的额外样式按需迁回，保留本课新增规则。

下一步：完成本页后继续第14页

## PPT第14页：确认终端目录与安装依赖

环境：**Windows PowerShell**。位置：**编辑器中新建PowerShell终端A**。

```powershell
$workRoot = Join-Path $env:USERPROFILE 'web-work'
Set-Location (Join-Path $workRoot 'p1-portfolio')
```

解释：即使新终端没有上页变量，也能准确进入个人工程。

```powershell
Get-Location
Test-Path ./package.json
```

解释：显示当前路径，并确认当前目录存在package.json。

成功标志：路径以p1-portfolio结尾，最后显示True。

遇到问题：False：回到项目根目录；不要在kit目录运行npm。

下一步：完成本页后继续第15页

## PPT第15页：安装并检查起步条件

环境：**Windows PowerShell**。位置：**p1-portfolio；PowerShell终端A**。

```powershell
npm.cmd ci
```

解释：按package-lock.json安装依赖，保证全班版本一致；需可用网络。

```powershell
npm.cmd run check:starter
```

解释：检查课堂文件与命令是否齐备，不检查TODO是否完成。

成功标志：依赖安装结束，起步检查通过。

遇到问题：ci失败：核对网络与锁文件；保留第一条错误，不自行升级依赖。

下一步：完成本页后继续第16页

## PPT第16页：启动网页并保持服务运行

环境：**Windows PowerShell**。位置：**p1-portfolio；PowerShell终端A**。

```powershell
npm.cmd run dev
```

解释：启动本地开发服务；运行期间这个终端会一直被占用。

解释：用浏览器打开终端显示的Local地址；第1—5课通常为http://127.0.0.1:5173/。

成功标志：页面能打开，看到本课起点。

遇到问题：端口占用：在旧服务终端按Ctrl+C后重启；第6课以Vite实际地址为准。

下一步：完成本页后继续第17页

## PPT第17页：接下来修改哪里

文件：scripts/theme-service.mjs, app.js, scripts/project-loader.mjs, projects.json
搜索标记：TODO-L5
保存快捷键：Ctrl+S。浏览器检查实际结果。
实操结束按页脚返回检查页。

下一步：完成本页后继续第18页

## PPT第18页：两个常见的使用问题

刷新后，用户选择的主题丢失。
读取数据失败时，页面没有清楚说明。
本课亲写主题记忆，并验证已提供的请求处理。

下一步：完成本页后继续第19页

## PPT第19页：前置检查：事件和状态

按钮被点击后，哪个事件会触发？
一个变量怎样表示浅色或深色？
刷新页面后，普通变量还保留吗？

下一步：完成本页后继续第20页

## PPT第20页：事件把用户操作交给函数

addEventListener 注册处理函数。
点击时才执行回调内部步骤。
先算下一个主题，再保存并应用。

```text
themeToggle.addEventListener("click", () => {
  const result = writeTheme(
    localStorage,
    nextTheme(document.documentElement.dataset.theme)
  )
  applyTheme(result.theme)
})
```

下一步：完成本页后继续第21页

## PPT第21页：主题只有两种有效状态

light 切换为 dark。
dark 切换为 light。
非法值先通过 normalizeTheme 退回 light。

```text
export function nextTheme(theme) {
  return normalizeTheme(theme) === "dark"
    ? "light"
    : "dark"
}
```

下一步：完成本页后继续第22页

## PPT第22页：浏览器保存键与字符串值

setItem 写入，getItem 读取。
本项目使用固定键 p1-theme。
值保存在当前站点对应的浏览器存储中。

```text
localStorage.setItem("p1-theme", "dark")
localStorage.getItem("p1-theme") // "dark"
```

下一步：完成本页后继续第23页

## PPT第23页：保存失败也要有可用界面

存储可能被限制或写入失败。
try 中尝试写入；catch 返回 saved:false。
无论是否保存，都返回可应用的主题。

```text
export function writeTheme(storage, theme) {
  const normalized = normalizeTheme(theme)
  try {
    storage?.setItem("p1-theme", normalized)
  } catch {
    return { theme: normalized, saved: false }
  }
  return { theme: normalized, saved: true }
}
```

下一步：完成本页后继续第24页

## PPT第24页：第一段操作：切换与记忆

打开：scripts/theme-service.mjs
定位：TODO-L5
修改或操作：实现nextTheme与writeTheme。
成功标志：点击后换色，刷新后保持。
完成后回到第25页检查。

下一步：完成后回到第25页检查

## PPT第25页：回到PPT：第1段检查

点击后换色，刷新后保持。
展示结果，并指出对应的代码或操作。
未达到：返回前一页修正，再进入下一步。

下一步：完成本页后继续第26页

## PPT第26页：三种数据位置

页面内存：当前页面运行期间的状态。
localStorage：当前站点在当前浏览器中的持久数据。
服务器数据库：通过服务端接口管理共享数据。

下一步：完成本页后继续第27页

## PPT第27页：fetch 请求的是一个资源

projects.json 是当前站点的静态文件。
fetch 发出请求，await 等待异步结果。
读取响应后，数据再交给页面处理。

```text
const response = await fetch("./projects.json")
if (!response.ok) throw new Error("读取失败")
const items = await response.json()
```

下一步：完成本页后继续第28页

## PPT第28页：四种请求状态

loading：请求尚未完成。
success：有数据可展示。
empty：请求成功，但没有记录。
error：请求或数据处理失败。

下一步：完成本页后继续第29页

## PPT第29页：用现成入口复现状态

普通地址：正常读取项目。
?empty=1：显示空状态。
?fail=1：请求不存在的文件，显示回退与重试。

下一步：完成本页后继续第30页

## PPT第30页：重试为什么可能仍失败

?fail=1 会持续选择不存在的资源。
保留该参数时点击重试，仍应显示失败回退。
删除参数并重新加载，恢复正常请求。

下一步：完成本页后继续第31页

## PPT第31页：第二段操作：请求与故障记录

打开：浏览器与app.js
定位：地址参数与Network请求
修改或操作：依次验证普通地址、?empty=1、?fail=1。
成功标志：显示空态或回退，去掉参数后恢复。
完成后回到第32页检查。

下一步：完成后回到第32页检查

## PPT第32页：回到PPT：第2段检查

显示空态或回退，去掉参数后恢复。
展示结果，并指出对应的代码或操作。
未达到：返回前一页修正，再进入下一步。

下一步：完成本页后继续第33页

## PPT第33页：存储失败的可重复测试

测试提供一个写入时抛错的对象。
函数应返回 saved:false。
返回的 theme 仍是有效主题。

```text
const brokenStorage = {
  setItem() { throw new Error("blocked") }
}
writeTheme(brokenStorage, "dark")
// { theme: "dark", saved: false }
```

下一步：完成本页后继续第34页

## PPT第34页：发布前恢复展示状态

保留正常 projects.json。
线上链接不要带课堂故障参数。
分别验证本地与公网主题记忆。
说明两处存储互不自动同步。

下一步：完成本页后继续第35页

## PPT第35页：个人变式任务

清除主题键后重新加载；模拟存储写入失败，页面仍能切换；验证 ?fail=1 与 ?empty=1。
先写预期，再动手修改，最后说明依据。

下一步：完成本页后继续第36页

## PPT第36页：新开检查终端，确认核心任务

环境：**Windows PowerShell**。位置：**p1-portfolio；PowerShell终端B**。

```powershell
$workRoot = Join-Path $env:USERPROFILE 'web-work'
Set-Location (Join-Path $workRoot 'p1-portfolio')
```

解释：终端A继续运行网页，终端B用于检查和Git操作。

```powershell
npm.cmd run check:lesson
```

解释：运行本课核心验收；TODO未完成时失败属于预期。

成功标志：显示本课验收通过。

遇到问题：失败：根据提示返回对应TODO修正，不直接改测试。

下一步：完成本页后继续第37页

## PPT第37页：结构检查与自动测试

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
npm.cmd run check
```

解释：检查本课页面或Vue结构约定。

```powershell
npm.cmd test
```

解释：执行已经提供的功能测试；检查正常、边界和失败情形。

成功标志：两项命令都成功结束。

遇到问题：只修复第一处相关错误后复测；不要把失败输出当作通过。

下一步：完成本页后继续第38页

## PPT第38页：生成并检查部署文件

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
npm.cmd run build
Test-Path ./dist/index.html
```

解释：build生成dist；Test-Path确认入口文件存在。

```powershell
Get-ChildItem ./dist
```

解释：列出构建产物，确认有index.html及页面需要的资源文件。

成功标志：构建成功并显示True，dist中有入口文件。

遇到问题：构建失败先检查第一条错误，不能上传不完整的dist。

下一步：完成本页后继续第39页

## PPT第39页：检查并暂存本课成果

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
git status --short
git diff
```

解释：查看未提交文件与已跟踪内容的变化；新文件在编辑器核对。

```powershell
git add .
git diff --cached --stat
```

解释：暂存本工程的改动，再概览将提交的文件。确认没有node_modules、dist或凭据。

成功标志：暂存列表只包含应提交的代码、配置和课堂说明。

遇到问题：nothing to commit：检查是否已提交；不要为了次数制造空提交。

下一步：完成本页后继续第40页

## PPT第40页：创建本地版本记录

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
git commit -m "feat: complete P1 lesson 05"
```

解释：commit创建本地记录；-m后面是本次修改说明。

```powershell
git status --short
git log -1 --oneline
```

解释：确认工作区干净，并显示刚生成的提交号。

成功标志：status无输出，log出现本课提交。

遇到问题：nothing to commit通常表示已经提交；先检查历史。

下一步：完成本页后继续第41页

## PPT第41页：合并本课分支并推送

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
git switch main
git merge --ff-only lesson-05
```

解释：回到main并快进合并本课提交；要求main没有独立分叉。

```powershell
git push origin main
```

解释：将更新后的个人主分支推送到同一个远程仓库，触发已有工作流。

成功标志：main已更新，push成功。

遇到问题：不能快进：用git log --oneline --graph --all查看分叉，请教师协助；不使用force。

下一步：完成本页后继续第42页

## PPT第42页：在网页启用或确认Pages

打开自己的GitHub仓库，点击Settings。
左侧选择Pages。
Build and deployment的Source选GitHub Actions。
这是网页操作，无需在终端输入菜单文字。

下一步：完成本页后继续第43页

## PPT第43页：检查自动构建与部署

点击仓库Actions，打开最新test-and-deploy-pages。
核对它对应刚才的commit。
verify与deploy都成功后，打开部署显示的URL。
设置Pages前首次运行失败时，配置后重跑任务。

下一步：完成本页后继续第44页

## PPT第44页：检查公开网址与完成本课

用另一个设备或未登录窗口打开部署URL。
检查个人文字、作品链接和本课新增功能。
提交公开URL、个人commit与离堂题回答。
普通课可先收本地成果，发布里程碑完整验收。

下一步：完成本页后继续第45页

## PPT第45页：离堂检查与下一步

概念题：页面内存、localStorage、服务器数据库有什么不同？
应用题：点击后变色但刷新丢失，可能少了哪一步？
把自己的一个错误写成“预期、实际、修复”。

下一步：本课完成

## 官方操作依据

- [GitHub Pages工作流](https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages)
- [Windows OpenSSH](https://learn.microsoft.com/en-us/windows-server/administration/openssh/openssh-overview)
- [Ubuntu Nginx](https://ubuntu.com/server/docs/how-to/web-services/install-nginx/)
