# 第02课 响应式与打印：操作命令逐条解释 v1.2

与本课44页PPT一一对应。按页序执行，单条命令失败时停止后续依赖步骤。用户工作目录为USERPROFILE下的web-work。

## PPT第1页：一页知我 · 第 02 课

响应式与打印
版本 v1.1
让同一份履历在手机、桌面和打印预览中都可阅读

下一步：完成本页后继续第2页

## PPT第2页：本课顺序与时间安排

0—10：看需求、查旧知。
10—25：讲解与最小示范。
25—50：第一段操作；50—60：针对卡点补讲。
60—100：第二段操作；100—120：测试与提交。
120—135：展示、解释与复盘。

下一步：完成本页后继续第3页

## PPT第3页：先识别当前操作环境

PS C:\…>：本机 Windows PowerShell。
本课命令统一在 PowerShell 中运行。
npm.cmd 与 npm 执行同一套npm命令。
Ubuntu 操作仅在第6课附录明确标注。

下一步：完成本页后继续第4页

## PPT第4页：课前安装与打开终端

Git：https://git-scm.com/downloads/win
Node.js：https://nodejs.org/en/download
VS Code：https://code.visualstudio.com/Download
安装 Node 24，安装后重开编辑器和 PowerShell。

下一步：完成本页后继续第5页

## PPT第5页：确认工具可用

环境：**Windows PowerShell**。位置：**任意目录**。

```powershell
git --version
```

解释：显示Git版本，确认可以领取和提交代码。

```powershell
node --version
npm.cmd --version
```

解释：分别显示Node与npm版本；Node按本课使用v24。

成功标志：出现版本号。

遇到问题：无法识别：安装工具后重开终端；不要继续clone或安装依赖。

下一步：完成本页后继续第6页

## PPT第6页：本课资源的准确地址

https://github.com/02-gdit-ffd-20260818/ffd-student-course-kit
本课分支：p1-guide-02-v1.2
点击仓库分支选择器，选中上述分支。
下一页开始领取；也可在Code菜单下载本分支ZIP。

下一步：完成本页后继续第7页

## PPT第7页：建立统一工作目录

环境：**Windows PowerShell**。位置：**任意目录**。

```powershell
$workRoot = Join-Path $env:USERPROFILE 'web-work'
```

解释：Join-Path拼接当前用户目录与web-work，适用不同用户名。

```powershell
New-Item -ItemType Directory -Force $workRoot
Set-Location $workRoot
```

解释：建立或复用工作目录，再进入它。-Force在此不会清空目录。

成功标志：终端位于用户目录下的web-work。

遇到问题：目录不可写：改用本人有权限的目录，后续保持一致。

下一步：完成本页后继续第8页

## PPT第8页：指定本课领取来源

环境：**Windows PowerShell**。位置：**web-work**。

```powershell
$repo = "https://github.com/02-gdit-ffd-20260818/"
$repo += "ffd-student-course-kit.git"
```

解释：两行拼成完整仓库地址。+=表示把后半段追加到变量。

```powershell
$branch = "p1-guide-02-v1.2"
$kit = "kit-02-v12"
```

解释：branch是本课分支，kit是本次领取目录；v12用于区别旧资料。

成功标志：三个变量已设置，未报错即可继续。

遇到问题：关闭过终端：从工作目录页重做变量设置。

下一步：完成本页后继续第9页

## PPT第9页：领取并核对资源

环境：**Windows PowerShell**。位置：**web-work**。

```powershell
git clone --single-branch --depth 1 `
  --branch $branch $repo $kit
```

解释：clone下载；--branch指定课次；--single-branch只取该分支；--depth 1只取最新提交。反引号续行，后面不能有空格。

```powershell
Get-ChildItem -LiteralPath $kit
```

解释：列出领取目录内容，核对README、操作命令和starter。

成功标志：本课目录出现，starter内有package.json。

遇到问题：目录已存在：打开README核对；不要删除学生成果或强行覆盖。

下一步：完成本页后继续第10页

## PPT第10页：继续个人工程，检查上一课记录

环境：**Windows PowerShell**。位置：**web-work**。

```powershell
Set-Location (Join-Path $workRoot 'p1-portfolio')
```

解释：进入上一次的个人工程；保留同一个.git与远程仓库。

```powershell
git status --short
git log -1 --oneline
```

解释：status查看未提交改动，log显示最近一次提交。

成功标志：status没有输出，log显示上一课成果提交。

遇到问题：有改动：先保存、检查并按本课后面的提交步骤提交，再升级。

下一步：完成本页后继续第11页

## PPT第11页：备份并建立本课分支

环境：**Windows PowerShell**。位置：**p1-portfolio**。

```powershell
git archive -o ../p1-before-02.zip HEAD
```

解释：把上次已提交的全部项目文件打成ZIP，保存在工程外；不含未提交文件。

```powershell
git switch main
git switch -c lesson-02
```

解释：切到主线，再新建本课开发分支。上一课已合并main后才执行。

成功标志：备份ZIP存在，分支为lesson-02。

遇到问题：分支已存在：用git branch检查，再git switch切到已有本课分支。

下一步：完成本页后继续第12页

## PPT第12页：确认干净后引入本课骨架

环境：**Windows PowerShell**。位置：**p1-portfolio**。

```powershell
if (git status --porcelain) { throw "先提交所有改动" }
```

解释：存在未提交内容则停止，本页后续复制不执行。备份上一课是必要前提。

```powershell
$source = "../kit-02-v12/starter"
Get-ChildItem -LiteralPath $source -Force |
  Copy-Item -Destination . -Recurse -Force
```

解释：复制本课文件，替换同名教学骨架。starter不含.git，个人Git历史继续保留。

成功标志：本课TODO与检查脚本已进入个人工程。

遇到问题：找不到source：核对领取目录；切勿改成教师答案目录。

下一步：完成本页后继续第13页

## PPT第13页：迁回自己的内容，再开始本课

打开 ../p1-before-02.zip 查看上一课文件。
本课：把姓名、介绍、技能和联系信息迁回index.html。
保留新骨架的履历区块与wide-card类名。
自己的额外样式按需迁回，保留本课新增规则。

下一步：完成本页后继续第14页

## PPT第14页：确认终端目录与安装依赖

环境：**Windows PowerShell**。位置：**编辑器中新建PowerShell终端A**。

```powershell
$workRoot = Join-Path $env:USERPROFILE 'web-work'
Set-Location (Join-Path $workRoot 'p1-portfolio')
```

解释：即使新终端没有上页变量，也能准确进入个人工程。

```powershell
Get-Location
Test-Path ./package.json
```

解释：显示当前路径，并确认当前目录存在package.json。

成功标志：路径以p1-portfolio结尾，最后显示True。

遇到问题：False：回到项目根目录；不要在kit目录运行npm。

下一步：完成本页后继续第15页

## PPT第15页：安装并检查起步条件

环境：**Windows PowerShell**。位置：**p1-portfolio；PowerShell终端A**。

```powershell
npm.cmd ci
```

解释：按package-lock.json安装依赖，保证全班版本一致；需可用网络。

```powershell
npm.cmd run check:starter
```

解释：检查课堂文件与命令是否齐备，不检查TODO是否完成。

成功标志：依赖安装结束，起步检查通过。

遇到问题：ci失败：核对网络与锁文件；保留第一条错误，不自行升级依赖。

下一步：完成本页后继续第16页

## PPT第16页：启动网页并保持服务运行

环境：**Windows PowerShell**。位置：**p1-portfolio；PowerShell终端A**。

```powershell
npm.cmd run dev
```

解释：启动本地开发服务；运行期间这个终端会一直被占用。

解释：用浏览器打开终端显示的Local地址；第1—5课通常为http://127.0.0.1:5173/。

成功标志：页面能打开，看到本课起点。

遇到问题：端口占用：在旧服务终端按Ctrl+C后重启；第6课以Vite实际地址为准。

下一步：完成本页后继续第17页

## PPT第17页：接下来修改哪里

文件：styles.css, index.html
搜索标记：TODO-L2
保存快捷键：Ctrl+S。浏览器检查实际结果。
实操结束按页脚返回检查页。

下一步：完成本页后继续第18页

## PPT第18页：同一份履历的三个阅读场景

手机：单列、文字不越界。
桌面：空间足够时分成两列。
打印：白底黑字，区块尽量完整。

下一步：完成本页后继续第19页

## PPT第19页：前置检查与起步

指出上次页面的 section 和 CSS。
用一句话解释 margin 与 padding。
运行 p1-portfolio，确认个人内容已迁入。

下一步：完成本页后继续第20页

## PPT第20页：用可观察的标准描述需求

390px 宽度无页面级横向滚动。
1280px 宽度出现两列。
打印预览文字清楚、链接可辨认。

下一步：完成本页后继续第21页

## PPT第21页：布局容器与网格项目

main 是网格容器。
它的直接子元素 section 成为网格项目。
gap 设置网格项目之间的间距。

下一步：完成本页后继续第22页

## PPT第22页：先完成桌面双列

宽度达到 700px 时启用网格。
两列各占一份空间。
minmax(0, 1fr) 允许列在约束内收缩。

```text
@media (min-width: 700px) {
  main {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 18px;
  }
  section { margin: 0; }
}
```

下一步：完成本页后继续第23页

## PPT第23页：需要跨列的区块

只有带 wide-card 类的区块跨满两列。
普通区块保持单列宽度。
样式选择器和 HTML 类名必须一致。

```text
@media (min-width: 700px) {
  .wide-card {
    grid-column: 1 / -1;
  }
}
```

下一步：完成本页后继续第24页

## PPT第24页：第一段操作：两种窗口宽度

打开：styles.css
定位：TODO-L2
修改或操作：添加700px双列规则与跨列规则。
成功标志：390px单列，1280px双列。
完成后回到第25页检查。

下一步：完成后回到第25页检查

## PPT第25页：回到PPT：第1段检查

390px单列，1280px双列。
展示结果，并指出对应的代码或操作。
未达到：返回前一页修正，再进入下一步。

下一步：完成本页后继续第26页

## PPT第26页：检查点：断点前后发生了什么

小于 700px：使用基础单列布局。
达到 700px：启用新增的网格规则。
浏览器按 CSS 条件匹配，不按设备名称判断。

下一步：完成本页后继续第27页

## PPT第27页：小屏幕减少非必要空间

宽度不超过 420px 时减小区块内边距。
保留文字与边缘的距离。
不通过把字号缩得很小来掩盖溢出。

```text
@media (max-width: 420px) {
  .site-header { padding-top: 44px; }
  section {
    padding: 18px;
    border-radius: 12px;
  }
}
```

下一步：完成本页后继续第28页

## PPT第28页：打印有独立的媒体规则

@media print 只在打印介质下应用。
打印时优先清晰信息，减少背景装饰。
长链接需要换行，才能在纸上保留用途。

下一步：完成本页后继续第29页

## PPT第29页：打印样式的最小切片

先处理颜色、布局和区块分页。
在打印预览验证，随后补齐页宽与链接样式。

```text
@media print {
  body { color: #000; background: #fff; }
  main { display: block; }
  section {
    break-inside: avoid;
    box-shadow: none;
  }
}
```

下一步：完成本页后继续第30页

## PPT第30页：第二段操作：完成打印版本

打开：styles.css
定位：末尾的@media print
修改或操作：补齐打印规则，对照课堂任务中的参考。
成功标志：打印预览正文完整，链接地址可读。
完成后回到第31页检查。

下一步：完成后回到第31页检查

## PPT第31页：回到PPT：第2段检查

打印预览正文完整，链接地址可读。
展示结果，并指出对应的代码或操作。
未达到：返回前一页修正，再进入下一步。

下一步：完成本页后继续第32页

## PPT第32页：常见错误与定位

两列没有出现：检查规则条件和选择器。
出现横向滚动：检查固定宽度与长内容。
打印区块缺失：检查 display:none 的作用范围。

下一步：完成本页后继续第33页

## PPT第33页：测试要覆盖介质

正常：桌面双列，内容层级清楚。
边界：窄屏与长经历仍可阅读。
失败检查：无效作品链接被记录并修复。

下一步：完成本页后继续第34页

## PPT第34页：个人变式任务

增加一条较长的经历，验证窄屏换行及打印区块完整性。
先写预期，再动手修改，最后说明依据。

下一步：完成本页后继续第35页

## PPT第35页：新开检查终端，确认核心任务

环境：**Windows PowerShell**。位置：**p1-portfolio；PowerShell终端B**。

```powershell
$workRoot = Join-Path $env:USERPROFILE 'web-work'
Set-Location (Join-Path $workRoot 'p1-portfolio')
```

解释：终端A继续运行网页，终端B用于检查和Git操作。

```powershell
npm.cmd run check:lesson
```

解释：运行本课核心验收；TODO未完成时失败属于预期。

成功标志：显示本课验收通过。

遇到问题：失败：根据提示返回对应TODO修正，不直接改测试。

下一步：完成本页后继续第36页

## PPT第36页：结构检查与自动测试

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
npm.cmd run check
```

解释：检查本课页面或Vue结构约定。

```powershell
npm.cmd test
```

解释：执行已经提供的功能测试；检查正常、边界和失败情形。

成功标志：两项命令都成功结束。

遇到问题：只修复第一处相关错误后复测；不要把失败输出当作通过。

下一步：完成本页后继续第37页

## PPT第37页：生成并检查部署文件

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
npm.cmd run build
Test-Path ./dist/index.html
```

解释：build生成dist；Test-Path确认入口文件存在。

```powershell
Get-ChildItem ./dist
```

解释：列出构建产物，确认有index.html及页面需要的资源文件。

成功标志：构建成功并显示True，dist中有入口文件。

遇到问题：构建失败先检查第一条错误，不能上传不完整的dist。

下一步：完成本页后继续第38页

## PPT第38页：检查并暂存本课成果

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
git status --short
git diff
```

解释：查看未提交文件与已跟踪内容的变化；新文件在编辑器核对。

```powershell
git add .
git diff --cached --stat
```

解释：暂存本工程的改动，再概览将提交的文件。确认没有node_modules、dist或凭据。

成功标志：暂存列表只包含应提交的代码、配置和课堂说明。

遇到问题：nothing to commit：检查是否已提交；不要为了次数制造空提交。

下一步：完成本页后继续第39页

## PPT第39页：创建本地版本记录

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
git commit -m "feat: complete P1 lesson 02"
```

解释：commit创建本地记录；-m后面是本次修改说明。

```powershell
git status --short
git log -1 --oneline
```

解释：确认工作区干净，并显示刚生成的提交号。

成功标志：status无输出，log出现本课提交。

遇到问题：nothing to commit通常表示已经提交；先检查历史。

下一步：完成本页后继续第40页

## PPT第40页：合并本课分支并推送

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
git switch main
git merge --ff-only lesson-02
```

解释：回到main并快进合并本课提交；要求main没有独立分叉。

```powershell
git push origin main
```

解释：将更新后的个人主分支推送到同一个远程仓库，触发已有工作流。

成功标志：main已更新，push成功。

遇到问题：不能快进：用git log --oneline --graph --all查看分叉，请教师协助；不使用force。

下一步：完成本页后继续第41页

## PPT第41页：在网页启用或确认Pages

打开自己的GitHub仓库，点击Settings。
左侧选择Pages。
Build and deployment的Source选GitHub Actions。
这是网页操作，无需在终端输入菜单文字。

下一步：完成本页后继续第42页

## PPT第42页：检查自动构建与部署

点击仓库Actions，打开最新test-and-deploy-pages。
核对它对应刚才的commit。
verify与deploy都成功后，打开部署显示的URL。
设置Pages前首次运行失败时，配置后重跑任务。

下一步：完成本页后继续第43页

## PPT第43页：检查公开网址与完成本课

用另一个设备或未登录窗口打开部署URL。
检查个人文字、作品链接和本课新增功能。
提交公开URL、个人commit与离堂题回答。
普通课可先收本地成果，发布里程碑完整验收。

下一步：完成本页后继续第44页

## PPT第44页：离堂检查与下一步

概念题：为什么断点取决于内容，而不只是某个手机型号？
应用题：margin 与 padding 分别改变哪里？
把自己的一个错误写成“预期、实际、修复”。

下一步：本课完成

## 官方操作依据

- [GitHub Pages工作流](https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages)
- [Windows OpenSSH](https://learn.microsoft.com/en-us/windows-server/administration/openssh/openssh-overview)
- [Ubuntu Nginx](https://ubuntu.com/server/docs/how-to/web-services/install-nginx/)
