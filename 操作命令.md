# 第06课 Vue作品门户：操作命令逐条解释 v1.2

与本课58页PPT一一对应。按页序执行，单条命令失败时停止后续依赖步骤。用户工作目录为USERPROFILE下的web-work。

## PPT第1页：一页知我 · 第 06 课

Vue作品门户
版本 v2.0
在已搭好的 Vue 工程中亲写作品卡片组件，保留旧功能并完成发布

下一步：完成本页后继续第2页

## PPT第2页：本课顺序与时间安排

0—10：看需求、查旧知。
10—25：讲解与最小示范。
25—50：第一段操作；50—60：针对卡点补讲。
60—100：第二段操作；100—120：测试与提交。
120—135：展示、解释与复盘。

下一步：完成本页后继续第3页

## PPT第3页：先识别当前操作环境

PS C:\…>：本机 Windows PowerShell。
本课命令统一在 PowerShell 中运行。
npm.cmd 与 npm 执行同一套npm命令。
Ubuntu 操作仅在第6课附录明确标注。

下一步：完成本页后继续第4页

## PPT第4页：课前安装与打开终端

Git：https://git-scm.com/downloads/win
Node.js：https://nodejs.org/en/download
VS Code：https://code.visualstudio.com/Download
安装 Node 24，安装后重开编辑器和 PowerShell。

下一步：完成本页后继续第5页

## PPT第5页：确认工具可用

环境：**Windows PowerShell**。位置：**任意目录**。

```powershell
git --version
```

解释：显示Git版本，确认可以领取和提交代码。

```powershell
node --version
npm.cmd --version
```

解释：分别显示Node与npm版本；Node按本课使用v24。

成功标志：出现版本号。

遇到问题：无法识别：安装工具后重开终端；不要继续clone或安装依赖。

下一步：完成本页后继续第6页

## PPT第6页：本课资源的准确地址

https://github.com/02-gdit-ffd-20260818/ffd-student-course-kit
本课分支：p1-guide-06-v1.2
点击仓库分支选择器，选中上述分支。
下一页开始领取；也可在Code菜单下载本分支ZIP。

下一步：完成本页后继续第7页

## PPT第7页：建立统一工作目录

环境：**Windows PowerShell**。位置：**任意目录**。

```powershell
$workRoot = Join-Path $env:USERPROFILE 'web-work'
```

解释：Join-Path拼接当前用户目录与web-work，适用不同用户名。

```powershell
New-Item -ItemType Directory -Force $workRoot
Set-Location $workRoot
```

解释：建立或复用工作目录，再进入它。-Force在此不会清空目录。

成功标志：终端位于用户目录下的web-work。

遇到问题：目录不可写：改用本人有权限的目录，后续保持一致。

下一步：完成本页后继续第8页

## PPT第8页：指定本课领取来源

环境：**Windows PowerShell**。位置：**web-work**。

```powershell
$repo = "https://github.com/02-gdit-ffd-20260818/"
$repo += "ffd-student-course-kit.git"
```

解释：两行拼成完整仓库地址。+=表示把后半段追加到变量。

```powershell
$branch = "p1-guide-06-v1.2"
$kit = "kit-06-v12"
```

解释：branch是本课分支，kit是本次领取目录；v12用于区别旧资料。

成功标志：三个变量已设置，未报错即可继续。

遇到问题：关闭过终端：从工作目录页重做变量设置。

下一步：完成本页后继续第9页

## PPT第9页：领取并核对资源

环境：**Windows PowerShell**。位置：**web-work**。

```powershell
git clone --single-branch --depth 1 `
  --branch $branch $repo $kit
```

解释：clone下载；--branch指定课次；--single-branch只取该分支；--depth 1只取最新提交。反引号续行，后面不能有空格。

```powershell
Get-ChildItem -LiteralPath $kit
```

解释：列出领取目录内容，核对README、操作命令和starter。

成功标志：本课目录出现，starter内有package.json。

遇到问题：目录已存在：打开README核对；不要删除学生成果或强行覆盖。

下一步：完成本页后继续第10页

## PPT第10页：继续个人工程，检查上一课记录

环境：**Windows PowerShell**。位置：**web-work**。

```powershell
Set-Location (Join-Path $workRoot 'p1-portfolio')
```

解释：进入上一次的个人工程；保留同一个.git与远程仓库。

```powershell
git status --short
git log -1 --oneline
```

解释：status查看未提交改动，log显示最近一次提交。

成功标志：status没有输出，log显示上一课成果提交。

遇到问题：有改动：先保存、检查并按本课后面的提交步骤提交，再升级。

下一步：完成本页后继续第11页

## PPT第11页：备份并建立本课分支

环境：**Windows PowerShell**。位置：**p1-portfolio**。

```powershell
git archive -o ../p1-before-06.zip HEAD
```

解释：把上次已提交的全部项目文件打成ZIP，保存在工程外；不含未提交文件。

```powershell
git switch main
git switch -c lesson-06
```

解释：切到主线，再新建本课开发分支。上一课已合并main后才执行。

成功标志：备份ZIP存在，分支为lesson-06。

遇到问题：分支已存在：用git branch检查，再git switch切到已有本课分支。

下一步：完成本页后继续第12页

## PPT第12页：确认干净后引入本课骨架

环境：**Windows PowerShell**。位置：**p1-portfolio**。

```powershell
if (git status --porcelain) { throw "先提交所有改动" }
```

解释：存在未提交内容则停止，本页后续复制不执行。备份上一课是必要前提。

```powershell
$source = "../kit-06-v12/starter"
Get-ChildItem -LiteralPath $source -Force |
  Copy-Item -Destination . -Recurse -Force
```

解释：复制本课文件，替换同名教学骨架。starter不含.git，个人Git历史继续保留。

成功标志：本课TODO与检查脚本已进入个人工程。

遇到问题：找不到source：核对领取目录；切勿改成教师答案目录。

下一步：完成本页后继续第13页

## PPT第13页：迁回自己的内容，再开始本课

打开 ../p1-before-06.zip 查看上一课文件。
本课：按下一行保留个人作品与页面文字。
作品写入public/projects.json和src/data/projects.js。
第6课首部、经历、署名分别修改Header、App、Footer。

下一步：完成本页后继续第14页

## PPT第14页：确认终端目录与安装依赖

环境：**Windows PowerShell**。位置：**编辑器中新建PowerShell终端A**。

```powershell
$workRoot = Join-Path $env:USERPROFILE 'web-work'
Set-Location (Join-Path $workRoot 'p1-portfolio')
```

解释：即使新终端没有上页变量，也能准确进入个人工程。

```powershell
Get-Location
Test-Path ./package.json
```

解释：显示当前路径，并确认当前目录存在package.json。

成功标志：路径以p1-portfolio结尾，最后显示True。

遇到问题：False：回到项目根目录；不要在kit目录运行npm。

下一步：完成本页后继续第15页

## PPT第15页：安装并检查起步条件

环境：**Windows PowerShell**。位置：**p1-portfolio；PowerShell终端A**。

```powershell
npm.cmd ci
```

解释：按package-lock.json安装依赖，保证全班版本一致；需可用网络。

```powershell
npm.cmd run check:starter
```

解释：检查课堂文件与命令是否齐备，不检查TODO是否完成。

成功标志：依赖安装结束，起步检查通过。

遇到问题：ci失败：核对网络与锁文件；保留第一条错误，不自行升级依赖。

下一步：完成本页后继续第16页

## PPT第16页：启动网页并保持服务运行

环境：**Windows PowerShell**。位置：**p1-portfolio；PowerShell终端A**。

```powershell
npm.cmd run dev
```

解释：启动本地开发服务；运行期间这个终端会一直被占用。

解释：用浏览器打开终端显示的Local地址；第1—5课通常为http://127.0.0.1:5173/。

成功标志：页面能打开，看到本课起点。

遇到问题：端口占用：在旧服务终端按Ctrl+C后重启；第6课以Vite实际地址为准。

下一步：完成本页后继续第17页

## PPT第17页：接下来修改哪里

文件：src/components/ProjectCard.vue, src/components/ProjectList.vue, src/App.vue, vite.config.js
搜索标记：TODO-L6
保存快捷键：Ctrl+S。浏览器检查实际结果。
实操结束按页脚返回检查页。

下一步：完成本页后继续第18页

## PPT第18页：重构的目标与不变的功能

页面组织改为 Vue 组件。
原来的作品、筛选、排序、主题、打印继续可用。
本课亲写一个组件，并验证完整门户。

下一步：完成本页后继续第19页

## PPT第19页：前置检查与工程起点

能解释对象、数组和渲染函数。
能在终端运行 npm 命令。
启动 p1-portfolio，先看到待完成卡片提示。

下一步：完成本页后继续第20页

## PPT第20页：单文件组件的三个部分

script setup：数据、函数与输入输出。
template：页面结构与绑定。
style：组件样式；本项目也复用全局样式。

下一步：完成本页后继续第21页

## PPT第21页：响应式数据怎样更新页面

ref 保存响应式值。
JavaScript 中通过 .value 访问。
模板中使用时通常自动解包。

```text
<script setup>
import { ref } from "vue"
const count = ref(0)
</script>
<template>
  <button @click="count++">{{ count }}</button>
</template>
```

下一步：完成本页后继续第22页

## PPT第22页：组件各自承担一个职责

App：组织状态与页面。
ProjectList：遍历作品并显示空态。
ProjectCard：展示一件作品并报告点击。
Header、Footer：复用页面首尾。

下一步：完成本页后继续第23页

## PPT第23页：props 把作品传给卡片

父组件通过 :project 传入对象。
子组件用 defineProps 声明输入。
子组件读取内容，改变父状态通过事件请求。

```text
<script setup>
defineProps({
  project: { type: Object, required: true }
})
defineEmits(["select"])
</script>
```

下一步：完成本页后继续第24页

## PPT第24页：模板绑定作品字段

双花括号输出文本。
:href 把表达式绑定到属性。
v-if 决定是否显示有效链接。

```text
<article class="project-card">
  <h3>{{ project.name }}</h3>
  <p>{{ project.summary }}</p>
  <a v-if="project.url" :href="project.url">
    查看项目
  </a>
</article>
```

下一步：完成本页后继续第25页

## PPT第25页：第一段操作：显示一件作品

打开：src/components/ProjectCard.vue
定位：TODO-L6
修改或操作：声明props并绑定名称、简介与技能。
成功标志：卡片显示传入的作品内容。
完成后回到第26页检查。

下一步：完成后回到第26页检查

## PPT第26页：回到PPT：第1段检查

卡片显示传入的作品内容。
展示结果，并指出对应的代码或操作。
未达到：返回前一页修正，再进入下一步。

下一步：完成本页后继续第27页

## PPT第27页：事件报告发生了什么

子组件发出 select，并带上作品 id。
父组件收到 id 后决定怎样更新状态。
事件名称与监听名称需要对应。

```text
<button
  type="button"
  @click="$emit('select', project.id)"
>
  记录查看
</button>
```

下一步：完成本页后继续第28页

## PPT第28页：列表转发事件到 App

ProjectList 遍历 items 并传入每条作品。
:key 使用稳定的 id。
select 事件逐级交给 App 处理。

```text
<ProjectCard
  v-for="project in items"
  :key="project.id"
  :project="project"
  @select="$emit('select', $event)"
/>
```

下一步：完成本页后继续第29页

## PPT第29页：已有函数继续服务于 Vue

filterProjects、sortProjects 仍处理数组。
computed 根据当前状态计算显示结果。
Vue 更新模板，业务函数仍可独立测试。

```text
const visibleProjects = computed(() =>
  sortProjects(
    filterProjects(projects.value, skill.value),
    order.value
  )
)
```

下一步：完成本页后继续第30页

## PPT第30页：第二段操作：点击与完整卡片

打开：src/components/ProjectCard.vue
定位：TODO-L6：事件按钮
修改或操作：补齐select事件，点击后观察父页面。
成功标志：父页面显示被点击作品的名称。
完成后回到第31页检查。

下一步：完成后回到第31页检查

## PPT第31页：回到PPT：第2段检查

父页面显示被点击作品的名称。
展示结果，并指出对应的代码或操作。
未达到：返回前一页修正，再进入下一步。

下一步：完成本页后继续第32页

## PPT第32页：重构需要回归检查

正常作品列表、筛选、排序。
主题切换与刷新保持。
空数据、请求失败与恢复。
手机布局、打印和链接。

下一步：完成本页后继续第33页

## PPT第33页：个人变式任务

给自己的一个作品修改简介；点击记录查看后父组件显示正确名称；检查筛选、主题、空态、打印没有倒退。
先写预期，再动手修改，最后说明依据。

下一步：完成本页后继续第34页

## PPT第34页：新开检查终端，确认核心任务

环境：**Windows PowerShell**。位置：**p1-portfolio；PowerShell终端B**。

```powershell
$workRoot = Join-Path $env:USERPROFILE 'web-work'
Set-Location (Join-Path $workRoot 'p1-portfolio')
```

解释：终端A继续运行网页，终端B用于检查和Git操作。

```powershell
npm.cmd run check:lesson
```

解释：运行本课核心验收；TODO未完成时失败属于预期。

成功标志：显示本课验收通过。

遇到问题：失败：根据提示返回对应TODO修正，不直接改测试。

下一步：完成本页后继续第35页

## PPT第35页：结构检查与自动测试

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
npm.cmd run check
```

解释：检查本课页面或Vue结构约定。

```powershell
npm.cmd test
```

解释：执行已经提供的功能测试；检查正常、边界和失败情形。

成功标志：两项命令都成功结束。

遇到问题：只修复第一处相关错误后复测；不要把失败输出当作通过。

下一步：完成本页后继续第36页

## PPT第36页：生成并检查部署文件

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
npm.cmd run build
Test-Path ./dist/index.html
```

解释：build生成dist；Test-Path确认入口文件存在。

```powershell
Get-ChildItem ./dist
```

解释：列出构建产物，确认有index.html及页面需要的资源文件。

成功标志：构建成功并显示True，dist中有入口文件。

遇到问题：构建失败先检查第一条错误，不能上传不完整的dist。

下一步：完成本页后继续第37页

## PPT第37页：检查并暂存本课成果

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
git status --short
git diff
```

解释：查看未提交文件与已跟踪内容的变化；新文件在编辑器核对。

```powershell
git add .
git diff --cached --stat
```

解释：暂存本工程的改动，再概览将提交的文件。确认没有node_modules、dist或凭据。

成功标志：暂存列表只包含应提交的代码、配置和课堂说明。

遇到问题：nothing to commit：检查是否已提交；不要为了次数制造空提交。

下一步：完成本页后继续第38页

## PPT第38页：创建本地版本记录

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
git commit -m "feat: complete P1 lesson 06"
```

解释：commit创建本地记录；-m后面是本次修改说明。

```powershell
git status --short
git log -1 --oneline
```

解释：确认工作区干净，并显示刚生成的提交号。

成功标志：status无输出，log出现本课提交。

遇到问题：nothing to commit通常表示已经提交；先检查历史。

下一步：完成本页后继续第39页

## PPT第39页：合并本课分支并推送

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
git switch main
git merge --ff-only lesson-06
```

解释：回到main并快进合并本课提交；要求main没有独立分叉。

```powershell
git push origin main
```

解释：将更新后的个人主分支推送到同一个远程仓库，触发已有工作流。

成功标志：main已更新，push成功。

遇到问题：不能快进：用git log --oneline --graph --all查看分叉，请教师协助；不使用force。

下一步：完成本页后继续第40页

## PPT第40页：在网页启用或确认Pages

打开自己的GitHub仓库，点击Settings。
左侧选择Pages。
Build and deployment的Source选GitHub Actions。
这是网页操作，无需在终端输入菜单文字。

下一步：完成本页后继续第41页

## PPT第41页：检查自动构建与部署

点击仓库Actions，打开最新test-and-deploy-pages。
核对它对应刚才的commit。
verify与deploy都成功后，打开部署显示的URL。
设置Pages前首次运行失败时，配置后重跑任务。

下一步：完成本页后继续第42页

## PPT第42页：检查公开网址与完成本课

用另一个设备或未登录窗口打开部署URL。
检查个人文字、作品链接和本课新增功能。
提交公开URL、个人commit与离堂题回答。
普通课可先收本地成果，发布里程碑完整验收。

下一步：完成本页后继续第43页

## PPT第43页：为已验收的版本建立标签

环境：**Windows PowerShell**。位置：**p1-portfolio；终端B**。

```powershell
git tag -a p1-v2.0 -m "P1 verified"
```

解释：给已验证的当前提交建立带说明的里程碑标签。

```powershell
git push origin p1-v2.0
```

解释：单独推送标签，让远程也能定位到这个版本。

成功标志：GitHub Tags页面出现p1-v2.0。

遇到问题：标签已存在：先git show p1-v2.0检查，不覆盖旧标签。

下一步：完成本页后继续第44页

## PPT第44页：离堂检查与下一步

概念题：组件从哪里得到 project？
应用题：点击事件怎样通知父组件？
把自己的一个错误写成“预期、实际、修复”。

下一步：本课完成；教师按需查看后面的Ubuntu参考

## PPT第45页：Ubuntu部署预演：教师按需演示

P1主课到上一页完成，以下为部署参考。
适用Ubuntu 24.04、已有SSH账号与sudo权限。
把已构建静态站部署到独立8088端口。
P2的API、数据库与HTTPS另按对应项目实施。

下一步：完成本页后继续第46页

## PPT第46页：从Windows检查SSH客户端

环境：**Windows PowerShell**。位置：**Windows PowerShell；任意目录**。

```powershell
ssh -V
```

解释：显示OpenSSH客户端版本；命令运行在Windows，不是服务器。

```powershell
Get-Command scp
```

解释：确认Windows能找到SCP文件传输工具。

成功标志：SSH版本和scp路径可见。

遇到问题：缺少客户端：Windows设置中搜索“可选功能”，安装OpenSSH客户端后重开终端。

下一步：完成本页后继续第47页

## PPT第47页：填写服务器并连接

环境：**Windows PowerShell**。位置：**Windows PowerShell；终端B**。

```powershell
$serverTarget = Read-Host "输入账号@服务器地址"
```

解释：例如老师提供的ubuntu@某IP；不要把密码或私钥粘进地址。

```powershell
ssh $serverTarget
```

解释：连接Ubuntu。首次核对老师提供的主机指纹后确认，按提示认证。

成功标志：提示符变为Ubuntu账号与主机名。

遇到问题：连接超时：核对地址与云安全组SSH端口；Permission denied先检查账号和密钥。

下一步：完成本页后继续第48页

## PPT第48页：确认Linux环境与目标端口

环境：**Ubuntu Bash**。位置：**Ubuntu Bash；登录后**。

```bash
whoami
pwd
```

解释：分别显示当前登录账号和当前目录，确认已经进入服务器。

```bash
cat /etc/os-release
sudo ss -ltnp "sport = :8088"
```

解释：查看Ubuntu版本与8088监听情况。ss只有表头时表示当前没有该端口监听。

成功标志：系统为预期Ubuntu，练习端口未被其他服务占用。

遇到问题：8088已占用：停止本次新站配置，由教师选择独立主机或统一新端口。

下一步：完成本页后继续第49页

## PPT第49页：安装Nginx并准备接收目录

环境：**Ubuntu Bash**。位置：**Ubuntu Bash；用户家目录**。

```bash
sudo apt update
sudo apt install -y nginx
```

解释：update刷新软件包索引；install安装Nginx；sudo申请管理权限。

```bash
mkdir -p ~/p1-upload
exit
```

解释：建立用户上传目录；exit结束SSH，返回Windows终端。

成功标志：安装成功，返回PS开头的Windows提示符。

遇到问题：无sudo权限或软件源失败：由服务器管理员处理，不继续写系统配置。

下一步：完成本页后继续第50页

## PPT第50页：从Windows上传构建结果

环境：**Windows PowerShell**。位置：**Windows PowerShell；p1-portfolio**。

```powershell
npm.cmd run build
Test-Path ./dist/index.html
```

解释：重新构建并确认入口，必须成功且为True后再上传。

```powershell
scp -r ./dist "${serverTarget}:p1-upload/"
```

解释：-r递归复制dist到远程用户的p1-upload目录；变量后的冒号用花括号分隔。

成功标志：SCP传输完成，没有错误。

遇到问题：serverTarget为空：重新Read-Host输入；上传的是dist而非node_modules。

下一步：完成本页后继续第51页

## PPT第51页：上传本站Nginx配置

环境：**Windows PowerShell**。位置：**Windows PowerShell；p1-portfolio**。

```powershell
scp ../kit-06-v12/deploy/nginx-p1.conf `
  "${serverTarget}:p1-upload/"
```

解释：上传本课提供的配置文件。反引号表示续行；此文件只服务本练习站点。

```powershell
ssh $serverTarget
```

解释：重新进入Ubuntu，后续sudo命令均在服务器运行。

成功标志：传输成功并进入Ubuntu提示符。

遇到问题：找不到配置：核对第6课领取目录和deploy子目录。

下一步：完成本页后继续第52页

## PPT第52页：Nginx配置文件的内容

这是配置文件内容，不是终端命令。
listen指定端口；root指定网站文件夹；index指定首页。
try_files按路径找文件，找不到时返回404。

```text
server {
  listen 8088;
  server_name _;
  root /var/www/p1-classroom;
  index index.html;
  location / { try_files $uri $uri/ =404; }
}
```

下一步：完成本页后继续第53页

## PPT第53页：安装本次站点文件

环境：**Ubuntu Bash**。位置：**Ubuntu Bash；用户家目录**。

```bash
ls -l ~/p1-upload/dist/index.html
```

解释：列出上传后的入口文件。文件不存在则停止，先修正Windows上传步骤。

```bash
sudo install -d -m 755 /var/www/p1-classroom
sudo cp -r ~/p1-upload/dist/. /var/www/p1-classroom/
```

解释：建立本站目录，再复制dist里的内容，避免多嵌套一层dist。仅对本练习目录执行。

成功标志：目录中可找到/var/www/p1-classroom/index.html。

遇到问题：入口不存在：先回Windows检查SCP路径；不要继续配置Nginx。

下一步：完成本页后继续第54页

## PPT第54页：设置可读权限并检查配置目标

环境：**Ubuntu Bash**。位置：**Ubuntu Bash；用户家目录**。

```bash
sudo chmod -R a+rX /var/www/p1-classroom
```

解释：让Nginx可读取本站文件并进入目录，范围限定为本练习站点。

```bash
ls -l /etc/nginx/sites-available/p1-classroom
ls -l /etc/nginx/sites-enabled/p1-classroom
```

解释：检查是否已有同名配置。首次练习应显示不存在；已有时先由教师确认归属。

成功标志：第一次配置时，两处同名文件均不存在。

遇到问题：已有配置：停止首次安装步骤；更新已有本站时先备份并检查差异。

下一步：完成本页后继续第55页

## PPT第55页：启用本站配置

环境：**Ubuntu Bash**。位置：**Ubuntu Bash；用户家目录**。

```bash
sudo install -m 644 ~/p1-upload/nginx-p1.conf \
  /etc/nginx/sites-available/p1-classroom
```

解释：复制配置到sites-available；Bash用反斜杠续行，不能用PowerShell反引号。

```bash
sudo ln -s /etc/nginx/sites-available/p1-classroom \
  /etc/nginx/sites-enabled/p1-classroom
```

解释：建立启用链接。只在上一页确认不存在同名配置后执行。

成功标志：两个路径指向本次配置文件。

遇到问题：File exists：不要用-f覆盖；回上一页确认已有配置。

下一步：完成本页后继续第56页

## PPT第56页：检查配置后再加载

环境：**Ubuntu Bash**。位置：**Ubuntu Bash；用户家目录**。

```bash
sudo nginx -t
```

解释：检查全部Nginx配置语法，必须看到successful后再运行下一条。

```bash
sudo systemctl reload nginx
curl -I http://127.0.0.1:8088/
```

解释：reload重新加载配置；curl -I仅请求响应头，检查本站是否返回HTTP 200。

成功标志：配置检查成功，本机HTTP响应为200。

遇到问题：语法检查失败则不reload；根据文件和行号修正。

下一步：完成本页后继续第57页

## PPT第57页：检查防火墙与访问条件

环境：**Ubuntu Bash**。位置：**Ubuntu Bash；用户家目录**。

```bash
sudo ufw status
```

解释：查看UFW状态。inactive时不为本课临时启用整个防火墙。

```bash
sudo ufw allow 8088/tcp
```

解释：仅当UFW已启用且教师确认需要开放本站时执行；允许HTTP练习端口。

成功标志：云安全组也已允许课堂需要的来源访问TCP 8088。

遇到问题：云安全组在云平台网页配置；本机curl成功不代表外网可达。

下一步：完成本页后继续第58页

## PPT第58页：公网验证与后续更新

Windows浏览器打开 http://服务器地址:8088/。
核对本课文字、资源、筛选和主题。
更新时重新build、上传dist，再复制本站文件。
仅静态内容更新通常不需要重载Nginx。

下一步：本节结束

## 官方操作依据

- [GitHub Pages工作流](https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages)
- [Windows OpenSSH](https://learn.microsoft.com/en-us/windows-server/administration/openssh/openssh-overview)
- [Ubuntu Nginx](https://ubuntu.com/server/docs/how-to/web-services/install-nginx/)
