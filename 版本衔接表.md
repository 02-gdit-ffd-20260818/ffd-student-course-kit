# P1 六版本衔接与个人内容保留

## 连续工程的原则

六个lesson目录是备课与补救用的检查点。学生日常维护一个个人Git仓库和一个固定网址。每课首先提交上一课工作，再创建本课分支；原.git不覆盖、不复制教师仓库历史。

学生基础较好时，从自己的上一版本增加本课文件；需要支架时，教师提供本课起步骨架并对照迁入个人内容。两种路径完成相同核心任务。起步骨架中的通用实现允许复用，本课TODO必须自己完成。

## 每次升级的操作

1. 保存并提交上一课成果，确认git status干净。
2. 建本课分支。用git show或编辑器比较保留旧版，不用复制多个“最终版”工作目录。
3. 在工作目录之外打开本课starter作为参考。按下面清单逐个引入新增/变化的技术文件；同时核对同课package.json、package-lock.json、scripts中的检查/构建文件和.github工作流。
4. 合并个人文字、经历、链接和数据。遇到同名文件，用差异视图保留自己的字段，不整目录覆盖。
5. 运行npm ci、check:starter、dev，再完成本课TODO。
6. 验证与提交完成后合并main、推送，原网站随版本更新。

## 六课文件地图

| 课次 | 起点与新增重点 | 个人内容怎样保留 | 本课最低实现 |
| --- | --- | --- | --- |
| 01 | 独立项目目录；index.html、styles.css、scripts、工作流 | 写入自己的公开介绍、技能、作品链接 | 两个section与个人内容 |
| 02 | 在v1.0加入履历结构与媒体查询 | 将旧标题、介绍、技能和联系方式合入本课index.html | 三类媒体样式 |
| 03 | 加入data.js、app.js、project-service.mjs；index加入容器和模块入口 | 把原作品的名称、简介、链接写进data.js；保留履历文本 | renderProjects |
| 04 | 加入筛选/排序控件与服务函数 | 保留data.js中的个人记录；核对控件id和app.js | filterProjects、sortProjects |
| 05 | 加入projects.json、主题服务、加载器及按钮 | 把个人作品同步到projects.json与data.js；后者作为回退 | nextTheme、writeTheme |
| 06 | 使用src、public与Vite骨架；index变成挂载入口 | 名片首部迁入ProfileHeader.vue，经历与联系迁入App.vue，署名迁入SiteFooter.vue；作品迁入public/projects.json和src/data/projects.js | ProjectCard组件 |

第03课实际路径是根目录data.js、app.js；第04课是scripts/project-service.mjs。本试教版不沿用旧导航PPT中的src/data.js、src/render.js等不匹配路径。

第06课旧根目录静态文件不再作为Vite页面入口。保留在Git历史中即可；若当前工作区仍有旧文件，先确认新构建不依赖它们，再由教师指导整理。不要为了“干净”删除.git、个人数据或未保存改动。

## 教师核对清单

- 学生的名字、经历和作品没有被示例“林晓”覆盖。
- 课堂起点能启动，待完成验收失败原因与本课TODO对应。
- 本课脚本、PPT路径和实际目录一致。
- 同一Git历史可以看到各课变化，而非六个互不关联的仓库。
- 普通课内容文件和回退文件一致；第06课发布使用dist。

需要快速恢复教学基线时，可另开本课starter副本做练习，原学生工程仍保留。完成后把本课关键实现与个人内容合回自己的分支，记录恢复原因。
