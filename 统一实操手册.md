# 第01课 名片上线：Windows 统一实操手册

领取初稿 → 运行网页 → 修改代码 → 检查 → Git 提交 → 推送 → 发布。适用 P1 v1.2；CMD 与 PowerShell 共用正文。

## 1. 打开终端，选定本课使用的类型

本手册把两种终端放进同一条课堂流程。老师演示 CMD 时，学生可跟随 CMD；使用 PowerShell 的学生，在同一步选择 PowerShell 框。不要两框都做，也不要做到一半换终端类型，变量只在当前窗口有效。

- CMD：点击 Windows 开始菜单，输入 cmd，打开“命令提示符”。或者在 VS Code 点击“终端 → 新建终端”，点击“+”旁小箭头，选择 Command Prompt。常见提示符是 C:\Users\你的用户名>。
- PowerShell：在 VS Code 的同一菜单选择 PowerShell。常见提示符是 PS C:\Users\你的用户名>。
- 普通窗口即可。点击终端最后一行粘贴命令，按 Enter；不要复制提示符本身。代码框按顺序逐框运行，有错误或 STOP 先停下。

“CMD / PowerShell 通用”的框，两种终端都能运行；命令注释写在框前，复制框内命令即可。“二选一”的框，各自保留 rem 或 # 注释，可以连同注释复制。含 if 或 for 的多行框要整体复制。出现提问时先输入自己的答案、回车，等提示符恢复后才复制下一框。

HTML、CSS、JavaScript、Vue 和 Markdown 开发代码粘贴到第 5 步指定的文件，不输入终端。Word 适合阅读；复制长命令可打开本课“统一实操手册.md”，在 VS Code 按 Ctrl+Shift+V 预览。

课堂节奏：PPT 展示任务 → 本手册完成当前步骤 → 核对成功标志 → 回到 PPT 看下一项。第 1—4 步领取与运行，第 5 步开发，第 6 步检查，第 7—9 步提交与推送，第 10 步发布。老师把实操时间留给学生，检查通过后再推进。

初次上课按本手册使用“用户目录/web-work/p1-portfolio”，先保持目录一致，方便老师检查。领取目录与个人工程并列，不在压缩包中或层层嵌套的资料目录里开发。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：任意目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：将当前 Windows 终端的输入输出代码页设为 UTF-8，方便显示中文。

```text
chcp 65001
```

成功标志：显示活动代码页为 65001。这只影响当前终端窗口。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：任意目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：显示 Git 版本，确认已安装。

```text
git --version
```

成功标志：出现版本号。如果无法识别，安装对应工具后重新打开当前终端。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：任意目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：显示 Node.js 版本，本课程使用 v24。

```text
node --version
```

成功标志：出现版本号。如果无法识别，安装对应工具后重新打开当前终端。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：任意目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：显示 npm 版本，npm.cmd 在两种 Windows 终端中都可以运行。

```text
npm.cmd --version
```

成功标志：出现版本号。如果无法识别，安装对应工具后重新打开当前终端。

安装入口：[Git for Windows](https://git-scm.com/downloads/win)、[Node.js](https://nodejs.org/en/download)、[VS Code](https://code.visualstudio.com/Download)。安装 Git 时保留 Git Credential Manager。课前准备 GitHub 个人账号，第一次推送时使用本人账号认证。

## 2. 建立工作目录，用 Git clone 领取本课

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：任意目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**二选一：完成自己终端对应的一框，然后跳到下方成功标志。**

**Windows CMD**（rem 为可复制注释）

```bat
rem 用变量记住本人的上课目录；set 后的整段双引号不会成为值的一部分。
set "courseRoot=%USERPROFILE%\web-work"
rem 如果目录不存在，就建立它；已有目录会保留。
if not exist "%courseRoot%" mkdir "%courseRoot%"
rem /d 同时切换盘符和文件夹，适用从其他盘符进入工程。
cd /d "%courseRoot%"
rem 不带参数的 cd 显示当前完整路径。
cd
```

**Windows PowerShell**（# 为可复制注释）

```powershell
# 使用本人的用户目录；已有目录保留，Force 不会清空它。
$courseRoot = Join-Path $env:USERPROFILE 'web-work'
New-Item -ItemType Directory -Force -Path $courseRoot
# 切换到工作目录，并显示完整路径。
Set-Location -LiteralPath $courseRoot
Get-Location
```

成功标志：最后显示的路径以 web-work 结尾。变量在当前终端窗口内有效，换窗口后按相应步骤重新设置。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：刚建立的 web-work。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**二选一：完成自己终端对应的一框，然后跳到下方成功标志。**

**Windows CMD**（rem 为可复制注释）

```bat
rem 这是老师的学生资源仓库地址，直接复制，不改成自己的成果仓库。
set "courseRepo=https://github.com/02-gdit-ffd-20260818/ffd-student-course-kit.git"
rem clone 下载资源；branch 指定第 1 课，single-branch 只取这一课，depth 1 只取最新提交。
rem 最后的 kit-01-v12 是下载到电脑后的文件夹名称。
git clone --branch p1-guide-01-v1.2 --single-branch --depth 1 "%courseRepo%" kit-01-v12
```

**Windows PowerShell**（# 为可复制注释）

```powershell
# 保存老师的资源地址；不是个人成果仓库。
$courseRepo = 'https://github.com/02-gdit-ffd-20260818/ffd-student-course-kit.git'
# branch 选择本课，single-branch 只取本分支，depth 1 只取最新提交。
# 最后一项是电脑上的领取目录。
git clone --branch p1-guide-01-v1.2 --single-branch --depth 1 $courseRepo kit-01-v12
```

解释：若提示目录已经存在，先查看其 README 核对课次，确认是本课资源才继续。不要删除已有成果或反复 clone。

成功标志：下载成功并返回提示符，目录内出现 kit-01-v12。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：web-work。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**二选一：完成自己终端对应的一框，然后跳到下方成功标志。**

**Windows CMD**（rem 为可复制注释）

```bat
rem 检查模板入口，READY 表示找到，STOP 表示找不到。
if exist "kit-01-v12\starter\package.json" (echo READY: starter found) else (echo STOP: starter not found)
```

**Windows PowerShell**（# 为可复制注释）

```powershell
# Test-Path 检查初稿入口；READY 才能继续。
if (Test-Path -LiteralPath 'kit-01-v12/starter/package.json') {
  Write-Output 'READY: starter found'
} else {
  Write-Output 'STOP: starter not found'
}
```

成功标志：显示 READY: starter found；STOP 时先检查领取结果和当前目录。

kit 是老师的领取目录；starter 是留有课堂任务的初稿；p1-portfolio 是自己的长期工程。只在自己的工程中开发并推送。

网络无法访问 GitHub 时，由老师发送 P1_第01课_学生资源_v1.2.zip。在文件资源管理器中右键选择“全部解压缩”，把 kit-01-v12 放进 web-work，确认里面直接有 starter/package.json，再继续第 3 步。首次安装依赖和远程推送仍需要网络。

## 3. 准备自己的工程

首次才复制。已有 p1-portfolio 时保留原工程，直接打开检查；下面两种终端的代码框都不会覆盖它。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：web-work。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**二选一：完成自己终端对应的一框，然后跳到下方成功标志。**

**Windows CMD**（rem 为可复制注释）

```bat
rem 只在目标不存在且模板完整时复制；STOP 时不执行复制。
if exist "p1-portfolio" (
  echo STOP: project already exists. Open your existing project.
) else (
  if not exist "kit-01-v12\starter\package.json" (
    echo STOP: starter is missing.
  ) else (
    rem xcopy 复制目录；E 包含空子目录，I 将目标视为目录，H 包含隐藏文件。
    xcopy "kit-01-v12\starter" "p1-portfolio" /E /I /H
  )
)
```

**Windows PowerShell**（# 为可复制注释）

```powershell
# 目标已存在时停止，避免覆盖自己的成果。
if (Test-Path -LiteralPath 'p1-portfolio') {
  Write-Output 'STOP: project already exists. Open your existing project.'
} elseif (-not (Test-Path -LiteralPath 'kit-01-v12/starter/package.json')) {
  Write-Output 'STOP: starter is missing.'
} else {
  # 只复制 starter；Recurse 包含子目录，Force 包含隐藏项。
  Copy-Item -LiteralPath 'kit-01-v12/starter' -Destination 'p1-portfolio' -Recurse -Force -ErrorAction Stop
}
```

解释：只复制 starter，会包含 .github 发布配置；老师仓库的 .git 在 kit 外层，不会复制进来。

成功标志：复制完成，web-work 下并列有 kit-01-v12 与 p1-portfolio。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：任意目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**二选一：完成自己终端对应的一框，然后跳到下方成功标志。**

**Windows CMD**（rem 为可复制注释）

```bat
rem 进入自己的工程；百分号变量自动替换成本人用户目录。
cd /d "%USERPROFILE%\web-work\p1-portfolio"
```

**Windows PowerShell**（# 为可复制注释）

```powershell
# 进入自己的工程；用户名含空格时也可使用。
Set-Location -LiteralPath (Join-Path $env:USERPROFILE 'web-work/p1-portfolio')
```

成功标志：提示符路径以 p1-portfolio 结尾。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：用 VS Code 打开当前工程，点号就是当前文件夹。

```text
code .
```

成功标志：左侧直接显示 package.json 与本课代码文件。

若 code 无法识别，手动打开 VS Code，选择“文件 → 打开文件夹”，进入用户目录下 web-work/p1-portfolio 并选择它。只看到一个 p1-portfolio 文件夹时，说明选中了上一级，应重新选择里面的工程。

## 4. 用一个终端运行网页

在工程窗口点击“终端 → 新建终端”，通过“+”旁的小箭头选择刚才使用的 Command Prompt 或 PowerShell。原来的领取窗口可以关闭。新终端中先重新进入工程：

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：任意目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**二选一：完成自己终端对应的一框，然后跳到下方成功标志。**

**Windows CMD**（rem 为可复制注释）

```bat
rem 新终端重新进入个人工程，不依赖上一窗口的临时变量。
cd /d "%USERPROFILE%\web-work\p1-portfolio"
```

**Windows PowerShell**（# 为可复制注释）

```powershell
# 进入自己的工程；用户名含空格时也可使用。
Set-Location -LiteralPath (Join-Path $env:USERPROFILE 'web-work/p1-portfolio')
```

成功标志：路径以 p1-portfolio 结尾。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：按锁文件安装依赖，等待结束后再输入下一条。

```text
npm.cmd ci
```

成功标志：命令正常完成；有错误先停止后续步骤。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：检查初稿是否齐备，此时允许 TODO 尚未完成。

```text
npm.cmd run check:starter
```

成功标志：命令正常完成；有错误先停止后续步骤。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：启动网页并持续运行，保持这个终端打开。

```text
npm.cmd run dev
```

成功标志：显示 Local 地址；用浏览器打开该地址，看到本课初稿。

第 1—5 课通常显示 http://127.0.0.1:5173/，第 6 课以 Vite 显示地址为准。服务运行时终端被占用是正常现象。先保持它运行，修改文件后 Ctrl+S 保存，再回浏览器 Ctrl+R 刷新。需要检查和 Git 操作时，回终端 Ctrl+C；如果询问是否终止批处理，输入 Y 并回车。

## 5. 具体开发：找到原文，再替换代码

通用手势：点击 VS Code 左侧“资源管理器”图标，展开目录，单击指定文件。按 Ctrl+F 搜索给出的标记，按 Esc 关闭搜索框；鼠标选中需要替换的原文，把代码粘贴到编辑区，按 Ctrl+S。不要把代码粘贴进搜索框或终端。

代码示范用于带做。先说明自己预期网页发生什么变化，再输入、保存并观察；完成后至少做一处个人修改，解释修改理由。

### 5.1 先改网页主标题，确认自己编辑的是正确文件

打开 index.html，Ctrl+F 搜索“你好，我是林晓”。只改标签之间的文字，例如把整行改成下面这样。周小禾是示例名，你可以使用自己的公开展示名。

```html
    <h1>你好，我是周小禾</h1>
```

保存并刷新，页面大标题应立即改变。若没变：先看编辑器是否打开 p1-portfolio，文件标签是否还有未保存圆点，再看浏览器是不是 终端显示的 Local 地址。

随后 Ctrl+F 搜索其余“林晓”，逐处修改 title、description 和 footer 中的展示名。只改文字，保留标签与属性引号。联系信息可以继续使用模板中的虚构演示邮箱。

### 5.2 修改 index.html

打开 p1-portfolio/index.html。搜索 TODO-L1，找到下面这段原文。只选中这段，不要选中相邻函数或其他区块。

修改前：

```html
<!-- TODO-L1: 在这里编写 about-title 对应的完整 section -->
```

替换成：

```html
<section aria-labelledby="about-title">
      <h2 id="about-title">关于我</h2>
      <p>我正在学习 HTML、CSS、JavaScript 与软件发布，希望成长为重视用户体验的前端开发者。</p>
    </section>
```

代码解释：section 是一个主题区块，h2 是区块标题。aria-labelledby 的值要与 h2 的 id 完全相同。p 中写自己的介绍。

按 Ctrl+S 保存，回浏览器 Ctrl+R 刷新，再做下方检查。

### 5.3 修改 index.html

打开 p1-portfolio/index.html。搜索 TODO-L1，找到下面这段原文。只选中这段，不要选中相邻函数或其他区块。

修改前：

```html
<!-- TODO-L1: 在这里编写 skills-title 对应的完整 section -->
```

替换成：

```html
<section aria-labelledby="skills-title">
      <h2 id="skills-title">正在掌握</h2>
      <ul class="tag-list">
        <li>语义化 HTML</li>
        <li>响应式 CSS</li>
        <li>Git 与 GitHub</li>
      </ul>
    </section>
```

代码解释：section 是一个主题区块，h2 是区块标题。aria-labelledby 的值要与 h2 的 id 完全相同。ul 是列表，每个 li 是一项技能，至少保留两项。

按 Ctrl+S 保存，回浏览器 Ctrl+R 刷新，再做下方检查。

### 5.4 修改 styles.css

打开 p1-portfolio/styles.css。搜索 section {，在这个样式区块内找到 padding: 24px;。只改这一行，不改其他选择器中的 padding。

修改前：

```css
padding: 24px;
```

替换成：

```css
padding: 28px;
```

代码解释：只替换 section { ... } 中这一行。padding 控制卡片内容与边框的距离，单位 px 是像素。

按 Ctrl+S 保存，回浏览器 Ctrl+R 刷新，再做下方检查。

### 5.6 逐项观察结果

页面出现“关于我”和“正在掌握”两个区块，至少两项技能。卡片内容距离边框比原来稍远。用 Tab 键移动到链接，应能看到黄色焦点框。第一段完成后回 PPT 第 23 页；间距与键盘检查完成后回第 27 页。

### 5.7 写下本次改动

在左侧打开 README.md，按 Ctrl+End 到文件末尾，新起一行追加下面这段。把示例中的说明改成你的实际操作，不要覆盖原有安装和运行说明。

```markdown
## 我的本课记录

展示名：周小禾
我修改的文件：填写实际文件名
修改目的：填写希望用户看到的变化
验证方法：填写自己实际执行的检查
结果：填写页面变化或错误修复情况
```

这一步让下一位同学或未来的自己能理解本次修改。填完 Ctrl+S 保存。

## 6. 停止网页，检查成果

在当前终端按 Ctrl+C，等提示符回来。在同一个终端逐条执行下列命令，每一条成功后再继续。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：检查本课自己编写的核心任务。

```text
npm.cmd run check:lesson
```

成功标志：正常完成；最后一项显示 READY: build found。失败时根据第一条错误回到代码修正，不改测试来绕过检查。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：检查工程约定的结构。

```text
npm.cmd run check
```

成功标志：正常完成；最后一项显示 READY: build found。失败时根据第一条错误回到代码修正，不改测试来绕过检查。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：运行已提供的测试。

```text
npm.cmd test
```

成功标志：正常完成；最后一项显示 READY: build found。失败时根据第一条错误回到代码修正，不改测试来绕过检查。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：生成 dist 部署文件。

```text
npm.cmd run build
```

成功标志：正常完成；最后一项显示 READY: build found。失败时根据第一条错误回到代码修正，不改测试来绕过检查。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**二选一：完成自己终端对应的一框，然后跳到下方成功标志。**

**Windows CMD**（rem 为可复制注释）

```bat
rem 确认生成的网页入口存在。
if exist "dist\index.html" (echo READY: build found) else (echo STOP: build missing)
```

**Windows PowerShell**（# 为可复制注释）

```powershell
# 检查部署入口是否已生成，READY 才能继续。
if (Test-Path -LiteralPath 'dist/index.html') {
  Write-Output 'READY: build found'
} else {
  Write-Output 'STOP: build missing'
}
```

成功标志：正常完成；最后一项显示 READY: build found。失败时根据第一条错误回到代码修正，不改测试来绕过检查。

## 7. 建自己的远程仓库，设置本地提交身份

在浏览器登录自己的 GitHub，打开 [新建仓库](https://github.com/new)。Owner 选本人；Repository name 填 p1-portfolio；选择 Public；README 不添加，.gitignore 和 License 选 None。点击 Create repository，复制 HTTPS 仓库地址。它必须属于本人，不能是老师的资源仓库。

回 index.html，把“查看代码仓库”链接中的 https://github.com/ 换成自己的仓库页面地址，保存并重跑第 6 步检查。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：首次建立个人 Git 仓库，主分支命名为 main。

```text
git init -b main
```

成功标志：初始化成功；已建过本项目仓库就保留原历史，跳过 init。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**二选一：完成自己终端对应的一框，然后跳到下方成功标志。**

**Windows CMD**（rem 为可复制注释）

```bat
rem 先清空临时变量，再询问本次提交署名。
set "displayName="
set /p "displayName=输入提交展示名后按回车: "
```

**Windows PowerShell**（# 为可复制注释）

```powershell
# 输入提交署名后回车；不是 GitHub 登录。
$displayName = Read-Host '输入提交展示名后按回车'
```

解释：CMD 的 set /p、PowerShell 的 Read-Host 都会停下来等待你的回答，不要把下一框的命令作为展示名输入。

成功标志：终端提问后，输入自己的提交展示名并回车；提示符重新出现才进入下一框。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**二选一：完成自己终端对应的一框，然后跳到下方成功标志。**

**Windows CMD**（rem 为可复制注释）

```bat
rem 先清空临时变量，再询问提交邮箱；可使用 GitHub noreply 地址。
set "commitEmail="
set /p "commitEmail=输入Git提交邮箱后按回车: "
```

**Windows PowerShell**（# 为可复制注释）

```powershell
# 输入提交邮箱后回车，可使用自己的 GitHub noreply 邮箱。
$commitEmail = Read-Host '输入Git提交邮箱后按回车'
```

成功标志：输入自己的提交邮箱并回车，提示符重新出现才进入下一框。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**二选一：完成自己终端对应的一框，然后跳到下方成功标志。**

**Windows CMD**（rem 为可复制注释）

```bat
rem 将展示名写入当前仓库，双引号保留姓名中的空格。
git config user.name "%displayName%"
rem 将邮箱写入当前仓库，这是提交署名信息，不是 GitHub 登录。
git config user.email "%commitEmail%"
```

**Windows PowerShell**（# 为可复制注释）

```powershell
# 设置当前仓库的提交署名，不影响其他工程。
git config user.name "$displayName"
# 设置当前仓库的提交邮箱。
git config user.email "$commitEmail"
```

成功标志：两项设置均未报错。署名和邮箱不是 GitHub 登录密码。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：列出改动文件，检查是否只包含预期内容。

```text
git status --short
```

成功标志：命令正常结束，没有错误。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：逐行查看已跟踪文件的变化，no-pager 让结果直接显示。

```text
git --no-pager diff
```

成功标志：命令正常结束，没有错误。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：将当前工程的变更放入暂存区。

```text
git add .
```

成功标志：命令正常结束，没有错误。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：检查将提交的文件摘要，不应包含 node_modules、dist 或凭据。

```text
git diff --cached --stat
```

成功标志：命令正常结束，没有错误。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：把暂存内容保存为一个本地提交，m 后面是提交说明。

```text
git commit -m "feat: complete P1 lesson 01"
```

成功标志：命令正常结束，没有错误。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：显示本地最新提交编号与说明，留待远程核对。

```text
git log -1 --oneline
```

成功标志：命令正常结束，没有错误。

## 8. 推送到自己的远程仓库

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**二选一：完成自己终端对应的一框，然后跳到下方成功标志。**

**Windows CMD**（rem 为可复制注释）

```bat
rem 清空变量，准备输入本人刚建立的远程仓库地址。
set "remoteUrl="
rem 运行下一条后粘贴自己的 HTTPS 地址并回车，不能输入老师仓库地址。
set /p "remoteUrl=粘贴自己的成果仓库HTTPS地址: "
```

**Windows PowerShell**（# 为可复制注释）

```powershell
# 回答时粘贴自己的 HTTPS 仓库地址，再按回车。
$remoteUrl = Read-Host '粘贴自己的成果仓库HTTPS地址'
```

解释：输入命令等待回答时，下一次粘贴的应该是地址，不是下一条命令。

成功标志：已粘贴本人仓库地址并按回车，提示符重新出现。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**二选一：完成自己终端对应的一框，然后跳到下方成功标志。**

**Windows CMD**（rem 为可复制注释）

```bat
rem 首次将自己的远程地址命名为 origin。
git remote add origin "%remoteUrl%"
```

**Windows PowerShell**（# 为可复制注释）

```powershell
# 首次把自己的远程仓库命名为 origin。
git remote add origin "$remoteUrl"
```

成功标志：未报错；已有 origin 时先核对地址，不重复添加。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：显示 fetch 和 push 地址，两者都应属于本人的成果仓库。

```text
git remote -v
```

成功标志：地址正确后才能推送。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：上传 main 的本地提交；首次的 u 参数建立远程跟踪关系。

```text
git push -u origin main
```

成功标志：推送成功，随后去 GitHub 核对。

首次可能出现 Git Credential Manager 登录窗口，选择 Sign in with your browser，核对本人账号，完成登录和认证，再回终端等待。终端要求 Password 时，不反复输入 GitHub 登录密码；先检查凭据助手安装及登录账号。

## 9. 核对远程，并再推送一次

浏览器打开自己的 GitHub 仓库，刷新 Code 页并选择 main。应能直接看到 package.json 和本课文件。打开一个刚修改的文件，确认内容已更新。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：显示本地完整提交编号。

```text
git rev-parse HEAD
```

成功标志：命令正常结束，没有错误。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：查询远程 main 的完整提交编号。

```text
git ls-remote origin refs/heads/main
```

成功标志：第一列提交号与上一条完全相同。

现在打开 README.md，在末尾写“我已完成第二次修改并核对远程提交”，Ctrl+S 保存，然后在同一个终端中执行：

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：只查看 README 的本次变化。

```text
git --no-pager diff -- README.md
```

成功标志：命令正常结束，没有错误。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：只暂存 README 文件。

```text
git add README.md
```

成功标志：命令正常结束，没有错误。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：为第二次修改建立新提交。

```text
git commit -m "docs: record second classroom update"
```

成功标志：命令正常结束，没有错误。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：把新提交推送到同一个远程仓库。

```text
git push origin main
```

成功标志：命令正常结束，没有错误。

刷新 GitHub 的 README 页面，应能看到新文字。日常开发就是修改、检查、add、commit、push，不需要每次重新 clone、init 或添加 origin。修改代码后需先重跑相应检查。

## 10. 发布网站、下课与下次继续

在自己 GitHub 仓库选择 Settings → Pages，Source 设为 GitHub Actions。到 Actions 打开 test-and-deploy-pages，确认 verify 和 deploy 成功，打开其部署网址。第一次在设置 Pages 前失败的运行，可设置后 Re-run all jobs。仓库代码页与 Pages 网站地址不同，作业分别提交两者以及最新 commit 编号。

下次同一台电脑，VS Code 打开原 p1-portfolio，选择本课使用的终端类型，按第 4 步重新运行即可。第 2—6 课要先按第 3 步备份、导入并恢复自己的内容。数据库继续按后续项目安排；Ubuntu 部署见“Ubuntu部署_统一实操”附录，服务器 Bash 步骤只执行一遍。

## 附录：常见终端卡点

找不到 npm.cmd：安装 Node.js 后重开当前终端。找不到 package.json：先检查是否位于自己的 p1-portfolio；cd /d 可以同时换盘和目录。

出现 #、$workRoot 或 Set-Location 无法识别：粘贴了 PowerShell 版，请选择本步骤 CMD 框。反过来，PowerShell 不直接使用 rem、set /p 或 cd /d。

CMD 代码框里的 %L 是交互式 CMD 写法。本手册要求直接粘贴到终端；自行改写批处理文件时，循环变量需写 %%L，调用 npm.cmd 等批处理还要使用 call。课堂先按终端操作，不另存为批处理运行。

远程地址填错：先确认现在位于自己的工程，再按以下两框修正。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**二选一：完成自己终端对应的一框，然后跳到下方成功标志。**

**Windows CMD**（rem 为可复制注释）

```bat
rem 清空变量后重新输入核对过的个人仓库地址。
set "correctUrl="
set /p "correctUrl=粘贴正确的个人仓库HTTPS地址: "
```

**Windows PowerShell**（# 为可复制注释）

```powershell
# 先运行本框，再粘贴核对过的本人仓库地址回答。
$correctUrl = Read-Host '粘贴正确的个人仓库HTTPS地址'
```

解释：先运行自己的输入框，等它提问，再粘贴地址回答。

成功标志：本人地址输入完成后再执行下一框。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**二选一：完成自己终端对应的一框，然后跳到下方成功标志。**

**Windows CMD**（rem 为可复制注释）

```bat
rem 修正远程地址后立即显示结果，核对属于本人。
git remote set-url origin "%correctUrl%"
git remote -v
```

**Windows PowerShell**（# 为可复制注释）

```powershell
# 修正已有 origin 地址。
git remote set-url origin "$correctUrl"
# 显示地址，再次核对属于本人。
git remote -v
```

成功标志：命令正常结束，没有错误。

推送出现 rejected / non-fast-forward：可能远程已有别的提交，不强制覆盖。先取回历史供老师核对：

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：取回远程历史，不覆盖当前工作文件。

```text
git fetch origin
```

成功标志：命令正常结束，没有错误。

运行平台：Windows 10/11 · CMD 或 PowerShell（按框内标签选择）。

运行位置：自己的 p1-portfolio 工程目录。

运行方法：点击已选终端最后一行，复制通用框或对应终端的一框，按 Enter。执行完核对结果，再向下继续；不要连续粘贴两种版本。

**CMD / PowerShell 通用，只执行一次。**

命令注释：显示最近分支历史，交给老师核对需要如何合并。

```text
git --no-pager log --oneline --graph --all -12
```

成功标志：命令正常结束，没有错误。


命令参考：[Microsoft xcopy](https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/xcopy)。PowerShell 的复制操作在本机独立演练核对。
