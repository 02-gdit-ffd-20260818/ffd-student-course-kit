# 第 3 课课堂任务：作品卡片与响应式布局

效果参考：https://ffd-p1-web-v2-20260918.netlify.app/lesson-03/

本课只修改两个文件：`index.html` 和 `lesson-03-task.css`。不要替换 `styles.css`。

## 任务 A：桌面端两列作品卡片

打开 `lesson-03-task.css`，搜索 `TODO A`：

1. 把 `display: block` 改成 `display: grid`。
2. 把 `grid-template-columns: 1fr` 改成 `grid-template-columns: repeat(2, minmax(0, 1fr))`。

成功标志：浏览器较宽时，四个作品显示为两列，图片完整出现。

## 任务 B：交互反馈与手机单列

继续搜索 `TODO B`：

1. 把 `transition: none` 改成 `transition: transform 180ms ease, box-shadow 180ms ease`。
2. 把 `transform: none` 改成 `transform: translateY(-4px)`。
3. 在 `@media (max-width: 640px)` 中，把两列改成 `1fr`。

成功标志：鼠标移到卡片上会轻微上移；开发者工具选择手机宽度后显示一列；页面没有横向滚动条。

## 保存版本

```text
git status
git diff
git add index.html lesson-03-task.css
git commit -m "完成第3课响应式作品布局"
git push
```

交付：提交编号、GitHub 仓库地址、GitHub Pages 网址，以及一句代码解释。
